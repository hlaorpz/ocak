# BRIEF — ADIM 4: REPOYA TAM TAŞIMA + CLAUDE.md + baglam.sh

**Tarih:** 8 Ağustos 2026
**Sahip:** CC
**Repo:** `~/Desktop/hlaorpz/ocak-site-clone` · dal `main`
**Bağımlılık:** ADIM 3 + 3b + B32 + B33 + B37 kapandı. ADIM 4 meşru (KARAR 460).
**Süre tahmini:** ~1,5 saat

Bu brief beş parçadır. **Parça 0 salt-read'dir ve tek başına koşulur** — rapor
gelir, DUR, onay bekle. Onay gelmeden Parça 1-4'e geçme (KARAR 355).

Bütün bash blokları `cd ~/Desktop/hlaorpz/ocak-site-clone` ile açılır.

---

## SAYIM KURALI — BU BRIEF'İN TAMAMINDA GEÇERLİ

Bu brief'te geçen her rakam, üretildiği yöntemle birlikte yazılmıştır. Senin
ürettiğin her rakam da öyle yazılacak: **eşik + araç + kaynak kümesi.**

Bir rakamı doğrulayamıyorsan yazma, ya da `TEYITSIZ` işaretle (KARAR 456).
Brief'teki bir rakam ölçümünle tutmuyorsa **brief yanılıyor olabilir** — DUR,
ikisini de raporla, düzeltme bende (KARAR 102).

**Bilinen tuzak — Türkçe metinde bayt ≠ karakter.** `mawk`'ta `length($0)` bayt
sayar ve `LC_ALL` bunu değiştirmez; macOS'un `awk`'ı sürüme göre karakter sayabilir.
`çığır` = 5 karakter, 9 bayt. Eşik tabanlı bir sayımda (`length < N`) bu fark
Türkçe metni sistematik olarak kaydırır. B39'un 231/232 vs 226/237 sapması bu
aileden. Parça 4'ün ilk işi bu probe'u koşup sonucu ölçüm dosyasının başına
yazmaktır.

---

## PARÇA 0 — ADIM 0 (SALT-READ, YAZMA YOK)

Hiçbir dosya oluşturma, değiştirme, silme. Yalnız oku ve raporla.

```bash
cd ~/Desktop/hlaorpz/ocak-site-clone
git status --short
git log --oneline -5
echo "--- docs ağacı ---"
ls -la docs/
ls -la docs/90-kronoloji/ docs/_uretilen/ docs/_arsiv/ docs/skills/ 2>&1
echo "--- kök ---"
ls -la | head -30
echo "--- CLAUDE.md var mı ---"
ls -la CLAUDE.md .claude/ 2>&1
echo "--- scripts ---"
ls -la scripts/ 2>&1
echo "--- gitignore ---"
cat .gitignore
```

Sonra şu yedi soruyu **ölçerek** cevapla:

**S1 — docs/ envanteri.** `docs/` altındaki her dosyanın adı + `wc -l` + `wc -c`.
Son baytı `0a` olmayan dosya var mı (`tail -c1 FILE | od -An -tx1`)? Varsa listele —
`wc -l` o dosyada fiziksel satırı bir eksik sayar.

**S2 — yedi `20-ref-*` teyidi.** Kaç tane var, adları ne? Beklenen yedi:
`site · protokoller · icerik-dili · notion · bot · program · marka`.

**S3 — `03-sira.md` var mı?** Varsa satır sayısı.

**S4 — `_uretilen/` içeriği.** Ne var? `.gitignore`'da mı? `site-icerik.md`
orada **yok** olmalı — teyit et.

**S5 — kaynak kanonu ve müfredat.** `docs/` altında hangi adla duruyorlar?
Project files'taki kopyalarıyla içerik eşdeğer mi:

```bash
cd ~/Desktop/hlaorpz/ocak-site-clone
find docs -iname "*kanon*" -o -iname "*mufredat*" -o -iname "*müfredat*"
# bulunan her dosya için:
wc -l -c <BULUNAN_YOL>
md5 <BULUNAN_YOL> 2>/dev/null || md5sum <BULUNAN_YOL>
```

**S6 — `2026-02.md` ve `00-devir.md` nerede?** Ledger'ın 77 satırı bu ikisine
işaret ediyor (`2026-02.md` 50 · `00-devir.md` 27). Dosya olarak repoda var mı,
hangi yolda?

```bash
cd ~/Desktop/hlaorpz/ocak-site-clone
find docs -name "2026-02.md" -o -name "00-devir.md"
```

**S7 — `ocak-site-icerik.md` repoda var mı?** Herhangi bir yolda, herhangi bir adla.
Ve onu üreten bir script var mı (`scripts/` altında Notion sayfa dump'ı çeken bir şey)?

```bash
cd ~/Desktop/hlaorpz/ocak-site-clone
find . -name "*site-icerik*" -not -path "./node_modules/*"
ls -la scripts/
grep -rln "site-icerik" scripts/ package.json 2>/dev/null
```

### Raporun sırası

1. **Ölçüm** — S1'in tam tablosu
2. **Envanter** — S2-S7'nin cevapları
3. **Çakışma listesi** — brief'in beklentisiyle dosyanın gerçeği arasındaki her fark

Sonra **DUR.** Onay bekle.

---

## PARÇA 1 — `CLAUDE.md`

**Yer:** repo kökü (`~/Desktop/hlaorpz/ocak-site-clone/CLAUDE.md`), `docs/` altı değil.
CC her oturumda kökü okur.

**Kısalık işlevdir.** Bu dosya her oturumda bağlama giriyor; uzarsa okunmaz hale gelir.
Aşağıdaki metin damıtılmış hâlidir — genişletme, örnek ekleme, açıklama yazma.
Gerekçeler `docs/20-ref-protokoller.md`'de yaşıyor, bu dosya oraya işaret eder.

Dosyayı **birebir** aşağıdaki içerikle oluştur:

````markdown
# CLAUDE.md — CC SABİT KURALLARI

Bu dosya her oturumda okunur. Kısalığı işlevidir.
Gerekçeler `docs/20-ref-protokoller.md`'de; burada yalnız kural durur.

## 1. ADIM 0 — salt-read önce (KARAR 355)

Her brief ADIM 0 ile açılır: oku, ölç, raporla, **DUR**, onay bekle.
Teşhis dosyadaki koda değil `npm run build` sonrası gerçek `dist/` çıktısına
dayanır. Layout/ritim işlerinde Chrome bağlanır, computed CSS'ten konuşulur —
statik CSS analizi tek başına yetmez (KARAR 419).

"Kod var" ≠ "output var". Sessiz fakirleşme en tehlikeli hata tipidir.

## 2. Gerçeklik spec'i ezer (KARAR 102)

Brief, doküman ve ledger yanılabilir. Ham kanıt üstündür.
Brief'in bir iddiası dosyanın gerçeğiyle çelişiyorsa: **DUR, ikisini de raporla,
düzeltmeyi bekle.** Kendi başına uzlaştırma.

## 3. Çapa disiplini (KARAR 465)

Patch çapası **tek satırdan** alınır ve dosyada **benzersiz** olmalıdır.
Blok-sonu dizeleri, girintili satırlar, birden çok yerde geçen ifadeler çapa olamaz.
Çapa tutmuyorsa durmak doğru reflekstir, hata değil.

Doğrulama kriteri ve sayı beyanı **dosyanın gerçek hâline karşı** yazılır,
beklentiden değil. `N → 0` biçimindeki grep kriterleri, aranan dizenin korunması
gereken tarihsel anlatımda da geçip geçmediği kontrol edilmeden yazılmaz.

## 4. Nicel iddia ölçülebilir olur (KARAR 470)

**(a) Ölçülemeyen rakam yazılmaz.** Dokümana giren her nicel iddia — satır sayısı,
kapsama oranı, "N vaka", "yaklaşık M" — üretilebilir bir komuta dayanmalıdır.
Dayanmıyorsa ya yazılmaz ya `TEYITSIZ` işaretlenir. Bellekten, önceki turdan ya da
brief'ten devralınan rakam ölçülmüş sayılmaz.

**(b) Rakam tek başına değil, yöntemiyle yazılır** — eşik, araç, kaynak kümesi.
"37 sığ satır" değil: "`kaynak` sütununda `^*.md:\d+$` eşleşen ve hedefi dönem
özeti bloğuna düşen 37 satır (`awk` + elle takip, 468 satırlık ledger)."

İki ölçüm çelişirse önce **tanımlar** karşılaştırılır, sonra rakamlar.
Çoğu çelişki tanım çelişkisidir.

*Vakalar: B32'de "~37 sığ satır" rakamının kaynağı bulunamadı; B39'da aynı ölçüm
iki kez yapıldı, 231/232 ve 226/237 verdi — fark karakter-vs-bayt sayımındandı;
B36'nın dört-biçim tablosu (418) ile bağımsız bir sayım (423) çelişti, kriter
yazılınca aynı çıktı.*

### Türkçe metinde bayt ≠ karakter

`mawk`'ta `length($0)` bayt sayar, `LC_ALL` değiştirmez. macOS `awk`'ı sürüme göre
karakter sayabilir. `çığır` = 5 karakter, 9 bayt. Eşik tabanlı sayımdan önce probe:

```bash
printf 'çığır\n' | awk '{print length($0)}'   # 5 → karakter · 9 → bayt
```

Sonucu ölçümün yanına yaz. Karakter sayımı gerekiyorsa `python3` ya da
`LC_ALL=C.UTF-8 wc -m` kullan, `awk` değil.

## 5. KIRPMA YASAĞI (KARAR 61)

İçerik silinmez, kırpılmaz, sadeleştirilmez. Yalnız **taşınır** ya da **dönüştürülür**.
Patch modu ekleme ve değiştirme yapar; çıkarma yapmaz.
Birleştirme yeniden yazımdır — ayrı karar ister.

Bir dosya dağıtıldığında üç şey aynı işin parçasıdır (KARAR 467):
(a) eşleme tablosu — kesim anında, sonradan üretilemez ·
(b) `01-kararlar.tsv`'nin o dosyaya bakan `kaynak` hücrelerinin dönüşümü ·
(c) kapsama + nokta örnekleme doğrulaması.

## 6. Commit disiplini

- Ayrı konu = ayrı commit. Mekanik dönüşüm ile semantik iş asla aynı commit'te olmaz.
- Commit ≠ deploy. "Push edildi" ayrı teyit ister; nihai teyit Vercel'de commit görünürlüğü.
- `--no-ff` merge kararı Kaan'ındır, sen vermezsin.
- Production'a giden merge (KARAR 388): kurtarma tag'i → `--no-commit --no-ff` dry-run →
  push'tan **önce** local `npm run build` + `vitest run` → yeşilse commit+push →
  gerçek iPhone Safari eyeball.
- Brief uygulandıktan sonra `.claude/notes.md`'ye brief adı + madde durumları +
  commit hash'leri yazılır. Yoksa sonraki oturum aynı brief'i ikinci kez koşar.

## 7. Tek klon (KARAR 463)

Bir repo, bir klon, bir aktif CC. Doküman brief'i koşarken eşzamanlı kod brief'i
verilmez — aynı repoda yaşıyorlar, birbirini beklerler.

## 8. Sır dokümanda yaşamaz (KARAR 469)

Token, API key, secret hiçbir doküman dosyasına yazılmaz — env'de ya da credential
store'da yaşar. Dokümanda yalnız yer tutucu durur:
`[TOKEN — n8n credential store, dokümanda tutulmaz]`.

Bir dosyada canlı sır görürsen: **DUR, yazma, Kaan'a bildir.** Rotate kararı onun.
Bu kural commit geçmişine de uygulanır — sır commit'lendiyse rotate etmek tek çözümdür,
dosyadan silmek yetmez.

## 9. Değişmeyen üç şey

- Her sayfa/konu ayrı sohbet (KARAR 52)
- ADIM 0 salt-read, agentlar dahil (KARAR 355)
- iPhone Safari eyeball — merge öncesi, otomatikleşmez

---

**Doküman haritası:** `docs/00-durum.md` (canlı, ≤200 satır) · `01-kararlar.tsv`
(ledger, indeks) · `02-borclar.md` · `03-sira.md` (kuyruk) · `10-marka.md` ·
`20-ref-*.md` (tema referansları, yedi dosya) · `90-kronoloji/YYYY-AA.md`
(append-only, asla yapıştırılmaz) · `_uretilen/` (script çıktısı) · `_arsiv/`.

Ledger **indekstir, referans değil** (KARAR 456). Bir kararın gerekçesi kronolojide
yaşar; tsv oraya işaret eder. tsv türetilmiş dosyadır — yanlışsa yeniden üretilir,
kaynak veriye dokunulmaz.
````

**Commit:** `docs: CLAUDE.md — CC sabit kuralları (KARAR 469, 470 dahil)`

---

## PARÇA 2 — `scripts/baglam.sh`

Profil bazlı bağlam paketi üretir, `pbcopy`'ye basar, manifesti stderr'e yazar.

**Neden yeniden kuruldu:** geçiş planındaki dört profil **beş** dosyaya göre yazılmıştı;
yedi var. Planın kendi sapma kaydı yalnız `20-ref-program.md`'yi yetim ilan ediyor.
Ölçtüm — **üç yetim var**: `program`, `marka` *ve* `notion`. (Planın `icerik`
profilindeki "taze notion dump" bir Notion export'udur, `20-ref-notion.md` değil;
`marka` profilindeki "marka" da `10-marka.md`'dir, `20-ref-marka.md` ayrı dosyadır.)

**Beşinci profil eklendi: `dokuman`.** Son üç tur doküman mimarisi işiydi ve o iş
için profil yoktu. Bu benim eklemem, planda yok — Kaan reddederse çıkar.

Dosyayı oluştur, `chmod +x` ver:

````bash
#!/usr/bin/env bash
# baglam.sh — profil bazlı bağlam paketi → pbcopy
# Çıktının ilk satırı manifesttir: Claude ne göremediğini bilir, uydurmaz.
set -euo pipefail

REPO="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"
D="$REPO/docs"

TUM="00-durum.md 01-kararlar.tsv 02-borclar.md 03-sira.md 10-marka.md \
20-ref-site.md 20-ref-protokoller.md 20-ref-notion.md 20-ref-icerik-dili.md \
20-ref-marka.md 20-ref-program.md 20-ref-bot.md CLAUDE.md"

case "${1:-}" in
  kod)
    ICERIR="CLAUDE.md 00-durum.md 01-kararlar.tsv 02-borclar.md 03-sira.md \
20-ref-site.md 20-ref-protokoller.md 20-ref-notion.md" ;;
  icerik)
    ICERIR="00-durum.md 03-sira.md 10-marka.md 20-ref-icerik-dili.md \
20-ref-program.md 20-ref-notion.md" ;;
  marka)
    ICERIR="10-marka.md 20-ref-marka.md 20-ref-program.md 20-ref-icerik-dili.md" ;;
  bot)
    ICERIR="00-durum.md 02-borclar.md 20-ref-bot.md 20-ref-notion.md" ;;
  dokuman)
    ICERIR="CLAUDE.md 00-durum.md 01-kararlar.tsv 02-borclar.md 03-sira.md \
20-ref-protokoller.md" ;;
  *)
    echo "kullanım: baglam.sh {kod|icerik|marka|bot|dokuman}" >&2
    exit 1 ;;
esac

# İÇERMEZ = TUM − ICERIR
ICERMEZ=""
for f in $TUM; do
  case " $ICERIR " in *" $f "*) ;; *) ICERMEZ="$ICERMEZ $f" ;; esac
done
ICERMEZ="$(echo "$ICERMEZ" | xargs)"

MANIFEST="PAKET: $1 · İÇERİR: $(echo $ICERIR | tr ' ' ',') · İÇERMEZ: $(echo $ICERMEZ | tr ' ' ','), 90-kronoloji/*, _arsiv/*, _uretilen/*"

# --- eksik dosya kontrolü: sessiz düşüş yasak ---
EKSIK=""
for f in $ICERIR; do
  [ "$f" = "CLAUDE.md" ] && P="$REPO/$f" || P="$D/$f"
  [ -f "$P" ] || EKSIK="$EKSIK $f"
done
if [ -n "$EKSIK" ]; then
  echo "HATA — profilde tanımlı ama diskte yok:$EKSIK" >&2
  echo "baglam.sh eksik dosyayla paket üretmez (sessiz düşüş yasağı)." >&2
  exit 2
fi

{
  echo "$MANIFEST"
  echo
  for f in $ICERIR; do
    [ "$f" = "CLAUDE.md" ] && P="$REPO/$f" || P="$D/$f"
    echo "===== $f ====="
    cat "$P"
    echo
  done
} | tee >(pbcopy) | {
  # stdout'u yutma, sadece ölç
  SATIR=0; BAYT=0
  while IFS= read -r l; do SATIR=$((SATIR+1)); BAYT=$((BAYT+${#l}+1)); done
  echo "$MANIFEST" >&2
  echo "→ pano: $SATIR satır (wc -l eşdeğeri), ~$BAYT bayt" >&2
}
````

```bash
cd ~/Desktop/hlaorpz/ocak-site-clone
chmod +x scripts/baglam.sh
for p in kod icerik marka bot dokuman; do
  echo "── $p ──"
  ./scripts/baglam.sh "$p" >/dev/null
done
```

Beş profil de hatasız koşmalı. Bir profil `HATA — profilde tanımlı ama diskte yok`
verirse **DUR ve raporla** — dosya adı yanlış olabilir ya da ADIM 0'da görülmeyen
bir dosya eksik. Kendi başına dosya adı düzeltme.

**Profil–dosya matrisi** (yedi `20-ref-*`'in hepsi en az bir profilde):

| dosya | kod | icerik | marka | bot | dokuman |
|---|:-:|:-:|:-:|:-:|:-:|
| CLAUDE.md | ● | | | | ● |
| 00-durum.md | ● | ● | | ● | ● |
| 01-kararlar.tsv | ● | | | | ● |
| 02-borclar.md | ● | | | ● | ● |
| 03-sira.md | ● | ● | | | ● |
| 10-marka.md | | ● | ● | | |
| 20-ref-site.md | ● | | | | |
| 20-ref-protokoller.md | ● | | | | ● |
| 20-ref-notion.md | ● | ● | | ● | |
| 20-ref-icerik-dili.md | | ● | ● | | |
| 20-ref-marka.md | | | ● | | |
| 20-ref-program.md | | ● | ● | | |
| 20-ref-bot.md | | | | ● | |

**Not — `marka` profilinde ledger yok.** Plan "kararlar(marka filtreli)" diyordu.
Filtre bugün yapılamaz: ledger'da tema sütunu yok, filtre `baslik` metnine dayanır
ve sessizce yanlış keser. Ledger ya tam girer ya hiç girmez; bu profilde hiç girmiyor,
`20-ref-marka.md` derinliği taşıyor. Tema sütunu açılırsa yeniden bakılır — bu bir
borç maddesidir (Parça 5'te açılıyor).

**Commit:** `docs: baglam.sh — beş profil, yedi 20-ref dosyasının tamamı kapsandı`

---

## PARÇA 3 — REPOYA TAM TAŞIMA

**Kritik ayrım:** project files'ı sen silemezsin — onlar Claude.ai proje bilgisinde,
dosya sisteminde değil. Senin işin **silme iznini üretmek**: repoda karşılığın var
olduğunu kanıtlayan bir rapor. Silmeyi Kaan UI'dan yapar.

Hedef (KARAR 455): ADIM 4 sonrası project files'ta yalnız `ocak-marka.md` kalır.

### Envanter ve karar

| project file | satır | karar | gerekçe |
|---|---|---|---|
| `ocak-kronoloji.md` | 5641 | SİL | ADIM 3'te dilimlendi → `90-kronoloji/` |
| `ocak-site-icerik.md` | 4821 | **repoya al, sonra SİL** | üretim yolu yok — aşağı bak |
| `ocak-referans.md` | 3574 | SİL | B32'de dağıtıldı → `20-ref-*` |
| `ocak-pilot.md` | 388 | SİL | ADIM 3'te bölündü → `_arsiv/ocak-pilot-v52.md` |
| `Ocak-Mufredat.md` | 275 | SİL | `docs/` altında duruyor; B39 dağıtım borcudur, kilit değil |
| `ocak-kaynak-kanonu.md` | 172 | SİL | aynı |
| `ocak-marka.md` | 236 | **KALIR** | KARAR 455 |

*Satır sayıları 8 Ağustos 2026 project files ölçümü, `wc -l`, yedi dosyanın da
son baytı `0a` (düzeltme gerekmedi).*

### `ocak-site-icerik.md` — üretim yolu yok, önce repoya alınır

Geçiş planı bu dosyayı `_uretilen/site-icerik.md`'ye, `.gitignore`'a, "script üretir"
notuyla gönderiyor. **Script yok** (Kaan sildi) ve `_uretilen/` içinde dosya yok.
Silinirse 4.821 satır repoda kopyasız ve üretimsiz kalır — KIRPMA YASAĞI'nı
doğrulanmamış bir varsayıma dayandırır.

Çözüm: dosyayı **artefakt olarak, `.gitignore`'suz** repoya al. Bayat olduğu başlıkta
yazılı olur. ADIM 5'te dump script'i yazılınca `.gitignore`'a alınıp bu kopya silinir.

Kaan bu dosyayı sana verecek (project files'tan kopyalayıp `~/Desktop/`'a bırakır).

```bash
cd ~/Desktop/hlaorpz/ocak-site-clone
mkdir -p docs/_arsiv
cp ~/Desktop/ocak-site-icerik.md docs/_arsiv/ocak-site-icerik-2026-08-04.md
wc -l -c docs/_arsiv/ocak-site-icerik-2026-08-04.md
```

Dosyanın **başına** şu blok eklenir (gövdeye dokunma):

```markdown
> **BAYAT ARTEFAKT — elle güncellenmez.**
> Notion dump'ı, son tazeleme `2026-08-04T17:06:37.718Z` (21 sayfa).
> Bu dosya türetilmiştir; kaynağı Notion'dur. Üretim script'i **yoktur** —
> ADIM 5'te yazılacak, o zaman bu kopya `_uretilen/`'e taşınıp `.gitignore`'a girer.
> Teşhis bu dosyaya değil `dist/`e dayanır (KARAR 355).
```

`.gitignore`'a **eklenmez** — bilerek versiyonlanıyor.

### `2026-02.md` ve `00-devir.md` — HEDEF YAPI'ya giriyorlar

Ledger'ın 77 satırı bu iki dosyaya işaret ediyor (`2026-02.md` 50 · `00-devir.md` 27;
ölçüm: `cut -f6 | çapa soy | sort | uniq -c`, 468 satırlık ledger). Geçiş planının
HEDEF YAPI'sı `90-kronoloji/` altında yalnız 2026-05/06/07/08 sayıyor — bu ikisi
mimaride yok. Sapma kaydı da görmemiş.

ADIM 0'da nerede olduklarını ölçtün. Bulundukları yer `docs/90-kronoloji/` ise iş yok,
yalnız Parça 5'teki sapma ekine yazılır. Başka yerdeyseler **taşıma, raporla** —
77 ledger satırının `kaynak` değeri onların yoluna bağlı, taşımak B33/B37 sınıfı bir
dönüşüm turu doğurur (KARAR 467) ve bu brief'in kapsamı değildir.

### Silme izin raporu

Rapor şu tabloyu üretir — Kaan bunu görüp project files'tan siler:

```bash
cd ~/Desktop/hlaorpz/ocak-site-clone
echo "=== SİLME İZİN RAPORU ==="
for f in "90-kronoloji" "_arsiv/ocak-pilot-v52.md" "20-ref-site.md" \
         "_arsiv/ocak-site-icerik-2026-08-04.md"; do
  echo "--- docs/$f ---"; ls -la "docs/$f" 2>&1 | head -20
done
echo "=== kanon + müfredat (ADIM 0 S5'te bulunan yollar) ==="
find docs -iname "*kanon*" -o -iname "*mufredat*" -o -iname "*müfredat*" | xargs wc -l
echo "=== ledger kaynak hedefleri yaşıyor mu ==="
cut -f6 docs/01-kararlar.tsv | tail -n +2 \
  | sed -E 's/#.*$//; s/:[0-9].*$//' | sed 's/[[:space:]]*$//' \
  | grep -E '\.md$' | sort -u \
  | while read -r p; do
      if [ -f "docs/90-kronoloji/$p" ] || [ -f "docs/$p" ]; then
        echo "YAŞIYOR  $p"
      else
        echo "ÖLÜ      $p"
      fi
    done
```

**Her `ÖLÜ` satırı silme iznini bloklar.** Ölü yol varsa DUR ve raporla —
KARAR 467 gereği dönüşüm ayrı tura ertelenemez, ama bu brief'in kapsamı da değil;
kararı Kaan verir.

**Commit:** `docs: site-icerik artefaktı arşive alındı + silme izin raporu`

---

## PARÇA 4 — B36 AÇILIŞ ÖLÇÜMÜ (AYRI COMMIT, MEKANİK)

**Bu B38 değildir.** B38 terminal denetimdir ve `02-borclar.md`'ye göre "ADIM 4-7
oturduktan sonra, doküman geçişinin **son** işi". Bu ölçüm B36'nın kendi açılış
şartıdır: *"B36 açılırken önce örneklem ölçümü yapılır; kuyruk boyutu ondan sonra
yazılır."* B36 bugün `~37 satır` diyor ve o rakamın kaynağının belirsiz olduğunu
kendisi yazıyor.

**Çıktı:** `docs/_uretilen/olcum-2026-08.md`

### 4.0 — Önce probe

```bash
cd ~/Desktop/hlaorpz/ocak-site-clone
echo "awk length testi: $(printf 'çığır\n' | awk '{print length($0)}')  (5=karakter, 9=bayt)"
awk --version 2>&1 | head -1 || awk -V 2>&1 | head -1
wc --version 2>&1 | head -1 || echo "BSD wc"
```

Bu üç satır ölçüm dosyasının **en başına** yazılır. Sonraki bütün rakamlar bu
ortamda üretilmiştir.

### 4.a — Mekanik çapa isabet oranı (sabit örneklem)

**Popülasyon:** `kaynak` sütunu `^[0-9A-Za-z-]+\.md:[0-9]+(,[0-9]+)*$` eşleşen satırlar.
Bu kriterle **418** çıkıyor — `02-borclar.md`'deki B36 tablosunun rakamıyla birebir.
Kod dosyaları (`.ts`, `.css`, `.astro`) ve `dist` artefaktları popülasyon dışıdır.

Örneklem **sabittir, sen seçmezsin.** Sistematik örnekleme: `no`'ya göre sıralı
418 satır, adım 20, ofset 10. Yeniden üretilebilir, taraf tutmaz.

| # | no | kaynak | başlık (kısalt.) |
|---|---|---|---|
| 1 | 11 | `2026-02.md:24` | Lokasyonlar |
| 2 | 31 | `2026-02.md:50` | Ürün/format tam haritası |
| 3 | 51 | `2026-05.md:19` | URL yapısı |
| 4 | 76 | `2026-05.md:51` | Kayıtlar database |
| 5 | 96 | `2026-05.md:94` | Site mimarisi — Astro SSG |
| 6 | 117 | `2026-05.md:193` | Lansman sonrası plan |
| 7 | 137 | `2026-05.md:2104` | Brief G.1-G.4 |
| 8 | 162 | `00-devir.md:475` | Instagram handle |
| 9 | 188 | `2026-05.md:3276` | Astro tooling |
| 10 | 210 | `2026-05.md:3565` | `ic-ses` kanonik section |
| 11 | 231 | `2026-06.md:298` | `Zoom Başlangıç Saati` |
| 12 | 256 | `2026-06.md:394` | GELaiL bit-bit korunur |
| 13 | 276 | `2026-06.md:423` | Arşivde "o günün gerçeği" |
| 14 | 297 | `2026-07.md:332` | iyzico → PayTR |
| 15 | 317 | `2026-07.md:364` | Çığlık → "sesin salınması" |
| 16 | 337 | `2026-07.md:467` | Detay sayfası |
| 17 | 358 | `2026-07.md:515` | Atölye Desen III |
| 18 | 381 | `2026-07.md:20` | Sabit değer reddi |
| 19 | 403 | `2026-07.md:1148` | Ürün-katmanı imza cümlesi |
| 20 | 425 | `20-ref-site.md:174` | `KayitBtn.astro` primitive |

*Örneklemin hedef dağılımı: 2026-05 (7) · 2026-07 (6) · 2026-06 (3) · 2026-02 (2) ·
00-devir (1) · 20-ref-site (1). Popülasyon dağılımına orantılı.*

**Her satır için:** çapanın gösterdiği satırı aç, ±5 satır oku, üçe ayır:

- **KENDİ** — o kararın kendi metni orada
- **KOMŞU** — başka bir kararın metni, ya da dönem özeti / sürüm listesi / indeks girdisi
- **HİÇ** — satır yok, boş, ya da tamamen ilgisiz

```bash
cd ~/Desktop/hlaorpz/ocak-site-clone
for pair in "11:2026-02.md:24" "31:2026-02.md:50" "51:2026-05.md:19" \
  "76:2026-05.md:51" "96:2026-05.md:94" "117:2026-05.md:193" \
  "137:2026-05.md:2104" "162:00-devir.md:475" "188:2026-05.md:3276" \
  "210:2026-05.md:3565" "231:2026-06.md:298" "256:2026-06.md:394" \
  "276:2026-06.md:423" "297:2026-07.md:332" "317:2026-07.md:364" \
  "337:2026-07.md:467" "358:2026-07.md:515" "381:2026-07.md:20" \
  "403:2026-07.md:1148" "425:20-ref-site.md:174"; do
  no="${pair%%:*}"; rest="${pair#*:}"; dosya="${rest%%:*}"; sat="${rest##*:}"
  yol="docs/90-kronoloji/$dosya"; [ -f "$yol" ] || yol="docs/$dosya"
  echo "═══ KARAR $no → $dosya:$sat ═══"
  sed -n "$((sat>5?sat-5:1)),$((sat+5))p" "$yol" | cut -c1-160
  echo
done
```

Sınıflandırma **senin yargın**. Kararsız kaldığın satırı `TEYITSIZ` yaz, tahmin etme.
Oranı üç kategoriyle ver: `KENDİ n/20 · KOMŞU n/20 · HİÇ n/20`.

### 4.b — `iliski` sütununda ok dışı içerik

**Bu ölçümün cevabı tanıma bağlıdır ve tek rakam yoktur.** Üç tanım kararı var;
üçünü de ayrı ayrı sayıp tabloyu ver, tek rakama indirme:

| tanım kararı | seçenek |
|---|---|
| T1 | `←307,332,335` (tek ok + virgüllü numara listesi) saf mı? |
| T2 | `⊂158` saf mı? (`⊂` şemada tanımsız beşinci sembol) |
| T3 | `B04` ilişki mi, içerik mi? |

```bash
cd ~/Desktop/hlaorpz/ocak-site-clone
python3 - <<'PY'
import csv, re, collections
rows=[r for r in csv.reader(open('docs/01-kararlar.tsv',encoding='utf-8'),delimiter='\t')][1:]
il=[r[4].strip() for r in rows]
print("toplam satır:", len(il), "· boş:", sum(1 for v in il if v==""))
dar   = re.compile(r'^[→←↔]\d+(\s*·\s*[→←↔]\d+)*$')
genis = re.compile(r'^[→←↔]\d+([,\s·]+[→←↔]?\d+)*$')
for ad, rx in (("DAR (yalnız okNNN, ayraç ·)", dar), ("GENİŞ (virgüllü liste de saf)", genis)):
    saf=sum(1 for v in il if v and rx.match(v))
    print(f"  {ad}: saf {saf} · ok-dışı {sum(1 for v in il if v)-saf}")
alt=collections.Counter()
for v in il:
    if not v or dar.match(v): continue
    if '⊂' in v: alt['⊂ (beşinci sembol)']+=1
    elif re.fullmatch(r'B\d+', v): alt['saf borç kodu BNN']+=1
    elif re.search(r'[→←↔]', v) and re.search(r'B\d+', v): alt['ok + borç + şerh']+=1
    elif re.search(r'[→←↔]', v): alt['ok + serbest şerh']+=1
    else: alt['hiç ok yok, saf metin']+=1
print("\nDAR tanıma göre ok-dışı olanların kırılımı:")
for k,n in alt.most_common(): print(f"  {n:4d}  {k}")
PY
```

### 4.c — `kaynak` sütunu biçim dağılımı

`02-borclar.md`'nin B36 tablosu dört biçim ve `418 · 35 · 8 · 7` diyor. Aynı sayımı
tekrarla ve **kriteri de yaz.** Tutmuyorsa hangi satırların kategori değiştirdiğini
listele — B36 tablosunu güncelleyeceğiz.

```bash
cd ~/Desktop/hlaorpz/ocak-site-clone
python3 - <<'PY'
import csv, re, collections
rows=[r for r in csv.reader(open('docs/01-kararlar.tsv',encoding='utf-8'),delimiter='\t')][1:]
c=collections.Counter(); orn=collections.defaultdict(list); uyum=[]
for r in rows:
    v=r[5].strip()
    if v=='': k='BOŞ (şema ihlali — kaynak zorunlu)'
    elif re.search(r'\.(ts|css|astro|mjs|js|sh|json)\b', v) or v.startswith('dist'):
        k='kod/dist artefaktı'
    elif re.search(r'^[0-9A-Za-z-]+\.md:\d+(,\d+)*$', v): k='mekanik :NNNN'
    elif '#' in v: k='elle konmuş # çapası'
    elif re.search(r'\.(md|tsv)$', v): k='çıplak dosya adı'
    else: k='SINIFLANDIRILAMADI'
    c[k]+=1
    if len(orn[k])<3: orn[k].append(f"{r[0]}: {v}")
    if k=='SINIFLANDIRILAMADI': uyum.append(f"{r[0]}: {v}")
for k,n in c.most_common():
    print(f"{n:5d}  {k}")
    for o in orn[k]: print(f"          {o}")
print("\nTOPLAM:", sum(c.values()))
if uyum:
    print("\n⚠ SINIFLANDIRILAMAYAN SATIRLAR (hepsi):")
    for u in uyum: print("  ", u)
PY
```

Ayrıca `#` çapalarını ikiye ayır: `#kNNN` biçiminde olanlar ve olmayanlar
(`#adim1`, `#NNN-blok`, `#41-madde5`). B36 tablosu 25/10 diyor.

### Ölçüm dosyasının biçimi

`docs/_uretilen/olcum-2026-08.md` şu sırayla yazılır:

1. **Ortam probe'u** (4.0 çıktısı) — sonraki her rakamın koşulu
2. **Popülasyon tanımı** — regex birebir, sonuç sayısı
3. **4.a** — 20 satırlık tablo (no · kaynak · sınıf · tek cümle gerekçe) + oran
4. **4.b** — üç tanım kararı, her biri için rakam, alt kırılım tablosu
5. **4.c** — biçim dağılımı + B36 tablosuyla fark listesi
6. **Sonuç** — üç cümle: B36 kuyruğunun ölçülen boyutu · `ocak-kararci` onarım
   modu gerekiyor mu · B38'e devreden ne

**Yorum yazma, ölçüm yaz.** "Kötü görünüyor" değil, "20'de 12". Karar Claude.ai'de.

**Commit:** `docs: B36 açılış ölçümü — çapa isabeti, iliski saflığı, kaynak biçimleri`
(kendi commit'i, Parça 1-3 ile karışmaz)

---

## PARÇA 5 — SAPMA KAYDI EKİ + LEDGER + BORÇ

Küçük ama atlanmaz: bu turda ölçülen sapmalar yazılmazsa bir sonraki tur aynı kör
noktayı miras alır.

### 5.1 — Geçiş planına sapma eki

`docs/2026-08-06-ocak-gecis-plani.md`'nin **sonuna** ekle (gövdeye dokunma):

```markdown
## SAPMA KAYDI — EK (8 Ağustos 2026, ADIM 4 hazırlığı)

Üstteki kayıt "aşağıdaki **beş** nokta" diyor, tablosu **altı** satır. Sayım
kendi gövdesiyle tutmuyor — KARAR 470'in ilk vakası, hem de nicel disiplini
uyaran belgede.

| # | üstteki kayıt ne diyor | ölçüm |
|---|---|---|
| 3-ek | `20-ref-program.md` hiçbir profile girmiyor | **üç yetim var**: program, marka *ve* notion. Planın `icerik` profilindeki "taze notion dump" bir Notion export'udur, `20-ref-notion.md` değil; `marka` profilindeki "marka" `10-marka.md`'dir |
| 6-ek | HEDEF YAPI dışında üç dosya (kanon 172 · müfredat 275 · sayfa-yazim-rehberi 219) | **beş** — `2026-02.md` ve `00-devir.md` de yok. Ledger'ın 77 satırı bu ikisine işaret ediyor (50 + 27; ölçüm: `kaynak` sütunu, çapa soyulmuş, 468 satır) |
| 7 | — | `durum` enum artık dokuz değerle ledger'da mühürlü (KARAR 456 tam metni), plan gövdesi yedi sayıyor |

**Ayrıca:** KARAR 465'in kendi `kaynak`'ı `2026-08.md` — satır numarasız, çapasız.
Çapa disiplinini mühürleyen karar sığ çapayla duruyor. Çıplak dosya adı taşıyan
yedi satırın hepsi 456-464 aralığında, yani doküman mimarisinin kendi kararları.
B36 kapsamı.
```

### 5.2 — `01-kararlar.tsv`'ye iki satır (append)

```
469	2026-08-08	Sır dokümanda yaşamaz — token/secret env'de ya da credential store'da	KALICI	←464	2026-08.md#k469
470	2026-08-08	Nicel iddia ölçülebilir olur — rakam üretim yöntemiyle yazılır	KALICI	←465 · ↔456	2026-08.md#k470
```

**469'un gerekçesi:** bu kural fiilen uygulanıyor (`20-ref-bot.md` BotZ token'ı
`[TOKEN — n8n credential store, dokümanda tutulmaz]` yer tutucusuyla duruyor;
KARAR 455 "private repo token'ı yapıştırma reddedildi" diyor) ama **ledger'da satırı
yoktu.** 464 numarası test ortamı TZ kararına aitti (`2026-08.md:25` + ledger birebir).
Sözlü olarak "KARAR 464 — sır dokümanda yaşamaz" diye anılıyordu; numara çakışmasıydı.

**470'in gerekçesi:** CLAUDE.md §4. Üç vaka: B32'nin kaynaksız "~37 sığ satır"ı,
B39'un 231/232 vs 226/237'si, B36 tablosunun (418) bağımsız bir sayımla (423)
çelişip kriter yazılınca uzlaşması.

⚠ `2026-08.md#k469` ve `#k470` çapaları **kronolojiye blok yazılmadan meşru değildir**
(KARAR 465). Blokları Claude.ai kapanış patch'inde verecek. Ledger satırlarını
şimdi yaz, çapa aynı patch'te doğar.

### 5.3 — `02-borclar.md`'ye iki borç

```markdown
## B40 — "KIRPMA YASAĞI 61/88" konvansiyonu yanlış

- [ ] **Sahip:** Claude.ai
- **Sorun:** Doküman genelinde KIRPMA YASAĞI `KARAR 61/88` diye anılıyor
  (`2026-08.md:130`, KARAR 457 metni dahil). Ledger:
  `61 = KIRPMA YASAĞI (KALICI)` · `88 = Çekirdek + arşiv ikili yapısı (SUPERSEDE)`.
  88 KIRPMA kararı değil, artık var olmayan bir dosya yapısının kararı, ve
  yürürlükten kalkmış. KARAR 462 zaten `88 → 145 → 397` zincirini kuruyor.
- **Neden borç:** kalıcı bir kural, SUPERSEDE bir karara bağlanmış görünüyor.
  Kırık değil — 61 doğru ve KALICI — ama konvansiyon yanlış yere işaret ediyor.
- **Eylem:** 88'in KIRPMA soyu var mı, tam metninden ölçülür (`2026-05.md:71`).
  Varsa "61/88" korunur ve 88'in ledger başlığı eksik demektir; yoksa doküman
  genelinde `KARAR 61` tekilleştirilir. CLAUDE.md bugün **61** yazıyor.
- **Kaynak:** ADIM 4 hazırlık ölçümü, 8 Ağustos 2026.

## B41 — Ledger'da tema sütunu yok, `marka` profili filtreleyemiyor

- [ ] **Sahip:** Claude.ai
- **Sorun:** Geçiş planı `baglam.sh marka` profilini "kararlar(marka filtreli)"
  olarak tanımlıyor. Ledger'da tema/alan sütunu yok; filtre `baslik` metnine
  dayanmak zorunda ve sessizce yanlış keser — bir marka kararı "marka" kelimesini
  içermeyebilir.
- **Karar (ADIM 4):** `marka` profiline ledger **hiç** girmiyor. Yarım filtre,
  filtresizden tehlikelidir (eksik olduğu görünmez).
- **Eylem:** ya yedinci sütun (`alan`) açılır — 468 satır elden geçer, pahalı —
  ya profil ledgersiz kalır. B38 ile aynı turda bakılabilir; ledger o zaman
  zaten satır satır denetlenecek.
- **Kaynak:** ADIM 4, `baglam.sh` profil tasarımı.
```

Ayrıca B36'nın "**⚠ Kuyruk boyutu ölçülmedi**" bloğunun altına tek satır:

```markdown
- **Açılış ölçümü yapıldı:** ADIM 4, `docs/_uretilen/olcum-2026-08.md` (8 Ağustos 2026).
```

**Commit:** `docs: sapma eki + KARAR 469/470 + B40/B41`

---

## COMMIT ÖZETİ

Beş ayrı commit, karıştırma:

1. `docs: CLAUDE.md — CC sabit kuralları (KARAR 469, 470 dahil)`
2. `docs: baglam.sh — beş profil, yedi 20-ref dosyasının tamamı kapsandı`
3. `docs: site-icerik artefaktı arşive alındı + silme izin raporu`
4. `docs: B36 açılış ölçümü — çapa isabeti, iliski saflığı, kaynak biçimleri`
5. `docs: sapma eki + KARAR 469/470 + B40/B41`

Push: `git push origin main`. `--no-ff` merge kararı yok — doğrudan `main`,
kod tarafına dokunulmuyor.

Bitince `.claude/notes.md`'ye: brief adı + beş maddenin durumu + commit hash'leri.

---

## DUR NOKTALARI

Şunlardan biri olursa **yazma, raporla**:

- ADIM 0'da brief'in beklentisiyle dosyanın gerçeği çelişiyor
- `baglam.sh` bir profilde eksik dosya buluyor
- Silme izin raporunda `ÖLÜ` yol çıkıyor
- `2026-02.md` ya da `00-devir.md` `docs/90-kronoloji/` dışında
- Ölçümde bir satırı sınıflandıramıyorsun
- Herhangi bir dosyada canlı token/secret görüyorsun (KARAR 469 — rotate kararı Kaan'da)
