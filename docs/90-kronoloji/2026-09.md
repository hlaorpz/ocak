# KRONOLOJİ — 2026-09 (Eylül)

**Kapsam:** N-Kolay turu — kart yüzeyinin mock'la geri açılması · `00-durum.md`'den
10 Eylül'de inen iki blok.
**Kaynak:** CC oturum ölçümleri + `.claude/notes.md`. Append-only, düzeltilmez (KARAR 61).
Ağustos dilimi: `2026-08.md`. Bu dosya **10 Eylül'de doğdu**.

---

## N-KOLAY TURU — KART YÜZEYİ MOCK'LA GERİ AÇILDI (10 Eylül 2026)

Zemin `7062846` (main, production — **15 gündür canlı olan deploy**; `9acbabb` değil).
Dönem zinciri: `bcc0196 → e03364d → 20fff25 → dd59c5d → 5a4c5bc → 0b173ac → 213ea0b →
4eb549b → fd946f3 → b1ee821`. On commit, **dokuzu kod** — 19 Ağustos'tan bu yana ilk kod
turu. Preview dalı `nkolay-test` (`f8409c4`, boş commit). Test **294/294 · 16 dosya →
325/325 · 19 dosya**.

**Zemin değişti:** N-Kolay sanal POS ile anlaşıldı. KARAR 488 kart akışını *silmeden*
kapatmıştı — gerekçesi tam da buydu: *"geri açmak bir env değişikliği + entegrasyon olsun,
arkeoloji olmasın."* On sekiz gün sonra sınandı ve **tuttu**: kart kodu yerindeydi, altı
tüketicinin altısı da anahtarın arkasında duruyordu, geri açmak tek anahtar meselesi oldu.

- **KARAR 573 — MUHAFIZIN KOŞULU (KALICI):** İŞ 2'de `/api/odeme-callback`'in 401
  muhafızı iki sabotajla sınandı. Birincisi — sağlayıcının fail-closed dalını fail-open'a
  çevirmek (`sır yoksa gecerli: true`) — üç testi kırmızıya düşürdü ve ölçümün çalıştığını
  gösterdi. İkincisi muhafızı silmedi, **devre dışı bıraktı**: `if (!dogrulama.gecerli)`
  yerine `if (false && !dogrulama.gecerli)`. **Suite yeşil kaldı.** Yalan söyleyen ölçüm
  sıra ölçümüydü: `handle()` gövdesinde `dogrulaCallback(` indeksinin `status: 401`
  indeksinden, onun da `await odemeyiOnayla(` indeksinden küçük olduğunu doğruluyordu. Üç
  iddia da hâlâ doğruydu — çünkü `status: 401` metni **ölü bloğun içinde aynen duruyordu**.
  Ölçüt muhafızın *orada olduğunu* görüyor, *çalıştığını* görmüyordu; bir `&&` kısa devresi
  kodu okuyan insana görünmezdi, ölçüme ise hiç görünmedi. Ölçüt koşulun kendisini arayacak
  biçimde sertleştirildi — `if (!dogrulama.gecerli) {` birebir aranıyor, `&&` ile
  sulandırılmış hâli ayrıca reddediliyor — ve aynı sabotaj tekrar koşuldu: **dört kırmızı.**
  Aynı sınıf commit 7'de bir kez daha çıktı: sayfa başlığı doğru metni gösterirken koşulu
  üçüzlemek (`kayitBulundu` yerine ikinci bir `durum === 'bulundu'`) davranışı bozmuyordu
  ama KARAR 395'i ihliyordu; ölçüt `durum ===` sayısını **tam bire kilitleyince** o da
  yakalandı. Genel kural: bir muhafızın testi, muhafızın kodda geçtiğini değil, **hangi
  koşulla** geçtiğini ölçmelidir. Varlık ölçümü `if (false && …)` ile susturulabilir;
  koşul ölçümü susturulamaz. `←567 · ↔395`

- **KARAR 574 — `src/pages/` TEST TUZAĞI (KALICI):** `/odeme/tamam`'ın yüzey testi önce
  `src/pages/odeme/tamam.test.ts` olarak yazıldı. `src/pages/` altındaki **her dosya Astro
  için bir route'tur**: dosya sessizce `tamam.test.astro.mjs` diye derlendi ve
  `__dirname is not defined in ES module scope` ile `npm run build`'i düşürdü —
  **`npx vitest run` 322/322 yeşil verirken.** `vitest.config.ts` `src/**/*.test.ts`
  topladığı için test oradan da koşuyordu; sorun yalnız derlemedeydi. Yani yeşil suite
  build'in sağlığını **kanıtlamaz**. Dosya `src/lib/odeme-tamam-yuzey.test.ts`'e taşındı.
  Repoda `src/pages/` altında hiç test olmamasının sebebi buymuş — on sekiz test dosyasının
  hepsi `src/lib/` ve `src/components/` altındaydı, ama **kural hiçbir yerde yazılı
  değildi**; boşluk kazayla korunuyordu. `b1ee821` ile `CLAUDE.md` §1'e girdi, "kod var ≠
  output var"ın tersi olarak: **test var ≠ build var.** `←355`

- **KARAR 575 — DENETİM PRODUCTION'DA KOŞAR (AKTIF):** İlk kurgu Production'ı kapalı
  tutuyordu; gerekçesi *"canlıda kayıt açık etkinlikler var, mock sağlayıcı ödendi der,
  gerçek bir kadın parasız Ödendi olur"* idi. **Kaan bu gerekçenin bu turda geçerli
  olmadığını ölçtü: kayıt henüz kadınlara açık değil.** Dolayısıyla parasız-Ödendi riski
  yok ve N-Kolay denetimi `www.ocak.biz` üzerinden yapılabilir — denetçinin gördüğü adres
  ile yasal metinlerin durduğu adres aynı olur, ki başvuru için asıl istenen buydu.
  Yapılandırma: `KART_AKISI=acik` · `PAYMENT_PROVIDER=mock`, `ODEME_CALLBACK_SIR`
  Preview'dakinden **farklı**. Bunun bedeli canlı bir sahte ödeme ekranıdır — borç olarak
  **B193** açıldı ve kapatma tetikleyicisi **AÇILIŞ'tan (24–27 Eylül) önce** diye bağlandı.
  ⚠ Kararın alındığı gün **uygulanmadı**: 11 Eylül ölçümünde `www.ocak.biz/cember/kayit`
  hâlâ kapalıydı (yöntem grubu markup'ı yok). Karar ≠ uygulama. `←488 · ↔B193`

- **KARAR 576 — İKİ YÖNTEM, ADSIZ SAĞLAYICI (AKTIF):** Üç yasal sayfada ödeme ve iade
  metinleri iki yöntemi birlikte kapsayacak hâle geldi. Sağlayıcı adı hiçbir metne
  girmedi — "lisanslı ödeme kuruluşu" yazıldı; ad, onay geldikten sonra tek turda girer,
  onay gelmezse sitede yalan kalmaz. İade cümlesi iki dosyada **birebir aynı 350 karakterlik
  dizeye** indi (B185). Beş yorum bloğu **geri konmadı, silindi** — içlerindeki taslaklar
  bugünün gerçeğini yansıtmıyordu: `gizlilik:148-153` *"bankamızın sanal POS altyapısı"*
  diyordu, oysa sağlayıcı bankamız değil ayrı bir ödeme kuruluşu; yorumu aynen geri koymak
  yanlış cümleyi canlıya taşırdı. Canlı iade cümlesi zaten yanlıştı: kart kapalıyken
  *"ödemenin yapıldığı kart veya yöntem"* diyordu — kart yokken yanıltıcı, kart gelince
  eksik. Bu commit `KART_AKISI`den **bağımsızdı**. `←488 · ←149`

- **KARAR 577 — BEYAN ÖLÇÜM DEĞİLDİR (KALICI):** İki vaka, ikisi de aynı turda.
  **(i)** claude.ai production deploy'unu ölçmeden `00-durum.md`'nin dönem HEAD satırından
  okudu ve `9acbabb` sandı. O satır **deploy değil dönem etiketidir** — KARAR 474 tam da
  bunu söylüyor. Gerçek: production `7062846` üstündeydi ve 15 gündür oradaydı.
  **(ii)** Kaan'ın *"eft kısımları gözükmüyor"* cümlesi *"havale seçenek olarak vardı"*
  diye okundu, ölçülmedi, ve CC brief'ine **"ZEMİN — bunu tartışma, ölçüm"** başlığıyla
  yazıldı. Başlığın işlevi durma refleksini bastırmaktı. Bisect yine de koşuldu ve ikisini
  de çürüttü: `KayitFormu.astro` `9acbabb`↔`7062846`↔`b1ee821` arasında **tek baytı
  değişmemişti**, eski production deploy'unun HTML'i çekildi ve yöntem grubunun orada da
  **hiç basılmadığı** görüldü. Görünen şey havalenin *sonucu*ydu — kademe blokları, tutar,
  ve submit sonrası IBAN. Regresyon yoktu. Kural: kullanıcının ya da asistanın cümlesi
  brief'e zemin diye yazılamaz; bir iddia ölçülmeden zemin olmaz. `←491 · ←564`

### KARAR'sız keşif kayıtları

- **`dogrulaCallback()` — KARAR 395'in uygulaması, yeni karar değil.** `/api/odeme-callback`
  19 Ağustos'tan 10 Eylül'e kadar **kimlik doğrulamasızdı**; adresi ve bir Notion sayfa
  UUID'sini bilen herkes bir kaydı Ödendi yapabilir, `kodKullanimArtir` üzerinden promo
  sayacını da şişirebilirdi. `KART_AKISI` kapalı olduğu için sömürülemedi. Doğrulama
  route'a değil **provider arayüzüne** girdi; N-Kolay onayı gelince yalnız yeni bir
  implementasyon eklenecek, route'a dokunulmayacak. Fail-closed: sır tanımsızsa `401`,
  gövde Notion'a **hiç** taşınmadan. Karşılaştırma sabit-zamanlı (SHA-256 + `timingSafeEqual`).
- **`/odeme/tamam` iki aydır kırıktı.** Sorgu `Referans No` rich_text alanını filtreliyordu;
  Kayıtlar DB'sinde böyle bir property **yok** — referans `Kayıt ID`/title'da yaşıyor
  (`Referans No` **Başvurular** DB'sinde). Notion 400 döndürüyor, catch onu **kayıt
  bulunamadı ile aynı boş nesneye** katlıyordu: bir hata ötekini saklıyordu, hatanın iki ay
  yaşamasının sebebi buydu. `durum` alanı üç hâli ayırdı ('bulundu'/'bulunamadi'/'hata'),
  sessiz düşüş kaldırıldı.
- **Türetilmiş yüzey kaynağın yerine geçmez** — üç vaka, aynı sınıf. Astro **hoisted
  script** `dist/` grep'ini yanıltır: `odeme_yontemi` çıktıda görünür ama
  `KayitFormu.astro_astro_type_script_*.js` içindedir, markup değil; render ölçümü `data-*`
  niteliğiyle yapılır. **Türkçe metin bundle'da Unicode kaçışlı yaşar**
  (`"\xD6demen alındı \xB7 OCAK"`) — kaçış çözülmeden yapılan grep **yokluk raporlar**;
  bu turda bir kez yalan söyledi ve düzeltildi. **`PUBLIC_HAVALE_IBAN` yerelde tanımsız** —
  yerel `dist/`ten IBAN teşhisi kurulmaz. Üçü de KARAR 577'nin ölçüm tarafındaki yüzü.
- **Uçtan uca tur Production'da koşuldu ve GEÇTİ** (11 Eyl, CC ölçümü). Kayıt →
  `/odeme/mock` → callback → Notion `Ödendi` → `/odeme/tamam`: beş adımın beşi de tuttu.
  `OCAK-5HNJ` referansıyla açılan satır **B192**'de adıyla kayıtlı. ⚠ **İŞ 2'nin muhafızı
  canlıda tuttu:** sırsız ve yanlış sırlı iki çağrı **401** aldı, gövde `sebep` sızdırmadı,
  **Notion'a dokunulmadı** — sabotaj testlerinin ölçtüğü davranış production'da birebir
  gerçekleşti. İki aylık `/odeme/tamam` hatası da canlıda kapandı: ekran Zoom linkini ve
  şifresini gösterdi. `bulunamadı` dalı da doğrulandı — `?ref=OCAK-YOK1` ve **ref'siz
  doğrudan URL** ikisinde de başlık, sekme başlığı ve gövde tutarlı.
- **İŞ 4 uçtan uca koşuldu** (10 Eyl, **Kaan'ın raporu** — CC bağımsız doğrulamadı):
  yöntem seçimi · `/odeme/mock` · callback · Notion `Ödendi` · `/odeme/tamam` ekranı.
  Notion'da kalan mock satırı **B192**.
- **Gecelik rebuild 10 Eylül'de yine `astro-iskelet` Preview'u üretti** — **B64 canlı
  olarak görüldü**, cron on beş gündür **ölü dala** basıyor (`notion-content-update`,
  commit `7bf265b`, 15 ardışık gün). Otomatik içerik deploy'u çalışmıyor ve çalışmadığı
  fark edilmiyor.
- **Vercel'de env yazmak build'i değiştirmez** — değer build zamanında sabitlenir, açmak
  için **redeploy** şart. Bu turda **iki kez** yaşandı: Production anahtarı yazılmıştı ama
  ilk iki ölçüm de sıfır döndü ve iki kez de "env build'e girmedi" sanıldı. Ayrım Preview'la
  yapıldı — orada aynı env çalışıyordu, yani mekanizma sağlamdı, eksik olan redeploy'du.
  Redeploy alınınca dört gate de açıldı (11 Eyl).
- **`nkolay-test` dalının ilk push'u Preview tetiklemedi** — Vercel ayarı "All unassigned
  git branches" olmasına rağmen. Boş commit `f8409c4` ile tetiklendi (**B191**).
- **`hlaorpz/ocak` reposu Public.** Site stealth (robots `Disallow`) ama kod açık.
  Bilinçli mi, **ölçülmedi** — N-Kolay denetimi bağlamında kayda değer.
- **`hlaorpz/ocak`'a bağlı ikinci bir Vercel projesi var** (`genuine-prosperity`),
  production deploy'u kırmızı. Ne olduğu ölçülmedi — **B190**.

---

## 11 AĞUSTOS TAHLİYESİ — ADIM 1–7 DALGALARI (10 Eylül'de indi)

*`00-durum.md`'nin `ŞU AN NEREDEYİZ` bölümünden KARAR 457/61 gereği taşındı — silinmedi.
Yerine tek satırlık işaretçi kaldı; üç canlı ayak (B36-b · B53/B51 · `baglam.sh`) o
satırda adıyla korundu.*

- **ADIM 1–6 ✅ (6–8 Ağu)** — ledger · Pilot bölünmesi · KARAR arkeolojisi · `CLAUDE.md`
  + `baglam.sh` · altı skill kadrosu. Tam kayıt **11 Ağustos tahliyesiyle**
  `90-kronoloji/2026-08.md`'ye indi (KARAR 457). Açık kalan tek ayak: **B36-b** (Claude.ai).
- **ADIM 7 birinci dalga ✅** — `mcp/` doğdu, Railway'de canlı, claude.ai'ye bağlı.
  Korpus **git deposundan** servis edilir (KARAR 479) — 105 dosya · 63 canlı · 42 arşiv
  (`docs_envanter`, `1d6726d`). Auth zorunlu; token URL yolunda — **ödün, B53'te görünür**.
- **ADIM 7 ikinci dalga — A+B ✅** — `docs_karar(no)` doğdu, dört araç oldu.
  Çapa sözleşmesi iki eksenli (KARAR 480); sığ çapa kendini bayrakla ilan ediyor
  (KARAR 481). `docs_envanter` artık kapsamını ve dağıtım ödününü söylüyor — **B54 ✅**.
  ⏸ **Kalan:** C parçası (B53, bağlantının başlığa geçmesi) claude.ai'de `Request headers`
  bölümü olmadığı için **düşürüldü**; D parçası (B51) ona bağlı olduğu için koşulmadı.
  İkisi de kendi hatlarında bekler. `baglam.sh` **emekli edilmedi**.

---

## KAPANAN HALKA — ETKİNLİK TARİHLERİ (10 Eylül'de indi)

*`00-durum.md`'nin `YAYINI KİLİTLEYENLER` bölümünden KARAR 457/61 gereği taşındı —
silinmedi. Yerine tek satırlık işaretçi kaldı; **B81** o satırda adıyla korundu.*

**Kapanan halka — etkinlik tarihleri.** 15 yayında etkinlik (6 Açık Kapı · 4 Çember ·
3 Seremoni · 1 Yolculuk · 1 Atölye), tarihli/saatli, ilki **31 Ağustos**; yedi
format kayıt route'u + `/anadolu/basvuru` + `/iletisim/bize-yaz` **canlı** (dumptan).
⚠ **"gövdeli" iddiası düzeltildi (24 Ağu):** on dördü gövdeli, biri değil — `yolculuk-acilis` `Detay` **NULL**, `Yayınla` ✓ (**B81**).

---

## `00-durum.md` BAŞLIĞINDAN İNEN BLOK (11 Eylül 2026)

`00-durum.md` başlık bölgesi KARAR 581'e (canlı dosya başlığı replace-only, en fazla
dört satır) göre boşaltıldı. Aşağıdaki paragraf o başlıktan **indirildi** — silinmedi,
taşındı (KARAR 61). 10 Eylül tahliyesinin kaydıdır ve Eylül dilimine aittir:

> *Şu an: 10 Eylül ölçümü — **B131 açık.** Bu turda iki blok indi: `ŞU AN NEREDEYİZ`in
> ADIM 7 dalgaları (canlı ayaklar B36-b · B53/B51 · `baglam.sh` satırda adıyla korundu) ve
> `Kapanan halka` etkinlik bloğu (B81 adıyla korundu). Tahliye tavanı açar, **kapağı
> kaldırmaz.***

Aynı başlıkta duran bir önceki tur adı da buraya iner — `00-durum.md` artık yalnız
**şu anki** turu taşır, tur zincirini değil:

> *10 Eylül 2026 · **N-Kolay turu — kart yüzeyi mock'la geri açıldı** — KARAR 573–577;
> on commit, dokuzu kod; B180 koruma ayağı ✅; B185–B193 açıldı. Önceki: 24 Ağustos
> (MJ görsel turu — V03 kapanışı, KARAR 568–572).*

---

## 2026-09-11 — ölçüm / yargı ayrımı turu · KARAR 578–582 · B194–B195 açıldı, kapanan yok · HEAD ed2f2d6

Zemin `dc74632`. Beş commit, **sıfırı kod** — `src/` ve `public/` bu turda hiç açılmadı.
Zincir: `3828f35 → 7047cc5 → 18a0289 → ed2f2d6 → (kapanış)`. Brief KARAR 103 kipindeydi:
kararlar Claude.ai'de mühürlendi, CC otonom koştu, Kaan bakmadı.

Bu başlık **KARAR 582'nin ilk uygulamasıdır**: tarih · tur adı · KARAR aralığı ·
borç hareketi · HEAD, sabit sırayla. Geriye dönük uygulanmaz (append-only, KARAR 61).

### Turun tek cümlesi

Şişmenin ve bayatlığın ortak kaynağı, **ölçümün yargıyla aynı satırda yaşamasıdır.**
Elle yazılan her rakam doğduğu anda bayatlar; yargı ise bayatlamaz, yalnız yanlışlanır.
İkisi ayrı dosyaya ayrıldı.

### Mühürlenen beş karar

- **KARAR 578 — DOKÜMAN SATIRI İKİ SINIFTIR (KALICI):** ölçüt tek sorudur — *bir komut
  bu satırı yeniden üretebiliyor mu?* Evet → **üretilendir**, elle yazılmaz, betikten
  doğar. Hayır → **yazılandır**; yargı, gerekçe, niyet, teşhis orada yaşar. Karma satır
  **bölünür**: rakamı üretilen tarafa, yargısı yazılan tarafa gider.
- **KARAR 579 — `docs/04-olcum.md` DOĞDU (AKTIF):** türetilmiş yüzey, `scripts/durum-uret.mjs`
  tamamen yeniden yazar. Elle düzenlenmez; yanlışsa betik düzeltilir ve yeniden koşulur
  (`05-harita.md` kural 1-b). **Saklanan sayaç kalktı** — `02-borclar.md` başlığındaki
  madde/açık/kapalı sayımı ve "sayım yöntemi" paragrafı üretilen yüzeye geçti; borç
  sayısı artık tek yerde ve türetilmiş.
- **KARAR 580 — CANLI HEAD İLE DÖNEM HEAD AYRI ŞEYLERDİR (KALICI):** canlı HEAD ölçüm
  anındaki `git log -1`'dir, **üretilendir**, `04-olcum.md`'de yaşar. Dönem HEAD kapanış
  commit'inden bir önceki commit'tir — bir **anlık görüntü etiketidir**, dönem kavramı
  yargıdır, **yazılandır**, `00-durum.md`'de kalır. KARAR 474 yürürlüktedir ve bununla
  çelişmez: betik dönem HEAD'e dokunmaz. İki satır yan yana durursa karıştırılır — bu
  yüzden ayrı dosyalarda yaşarlar.
- **KARAR 581 — CANLI DOSYA BAŞLIĞI REPLACE-ONLY, EN FAZLA DÖRT SATIR (AKTIF):**
  `00-durum.md` · `02-borclar.md` · `03-sira.md` başlık bölgesi (dosya başından ilk
  `---`'a kadar) tarih · tur adı · (varsa) dönem HEAD taşır, başka hiçbir şey.
  Başlıkta yaşayamaz: `önceki:` zinciri · ölçüm paragrafları · sayaç · tarihçe anlatısı.
  Başlığın işi *"şu an ne doğru"*, *"her ne doğru olduysa"* değil.
- **KARAR 582 — KRONOLOJİ TUR BAŞLIĞI SABİT BİÇİM TAŞIR (AKTIF):**
  `## YYYY-AA-GG — tur adı · KARAR n–m · B açılan/kapanan · HEAD xxxxxxx`. Gerekçe:
  kronoloji **ayına** göre dilimlenmiş ama arama **konuya** göre yapılıyor; KARAR numarası
  başlıkta geçerse `docs_ara` doğrudan turun gövdesine iner.

### Hangi satır hangi sınıfa gitti, neden

**Üretilene geçti** (`00-durum.md`'de yerine tek satırlık `→ docs/04-olcum.md` işaretçisi):
canlı HEAD · Test · Build sayımı · Vercel kimliği (team · project ID · proje adı) ·
`main` ↔ `astro-iskelet` commit farkı. Beşi de bir komutun çıktısıdır.

**Yazılanda kaldı:** dal modeli yorumu (*"ölü dal"*) · çalışma dizini · robots kararı ·
kanonik adres · deploy hook teşhisi (**B64**) · ödeme (KARAR 488) · referans kodu doktrini ·
ödeme onayı teşhisi · MailerLite otomasyon durumu · callback güvenliği. Hiçbiri komutla
üretilemez; hepsi bir yargıdır.

**Bölündü** (KARAR 578'in karma-satır kuralı): **dal modeli** — commit farkı üretilene,
*"ölü dal, main'in atası"* yargısı yazılana. **Vercel** — kimlik üretilene, B179 teşhisi
yazılana. **MailerLite** — burada kural **tersine** işledi, aşağıya bak.

### Ölçüm brief'i iki yerde düzeltti (KARAR 102 · 470)

1. **Dal farkı bayatmış.** `00-durum.md` *"85 commit geride"* diyordu.
   `git rev-list --count astro-iskelet..main` → **163**, `main..astro-iskelet` → **0**.
   Satır elle tutulduğu için sessizce iki katına yaklaşmış. Artık üretiliyor.
2. **Brief "on ölçüm paragrafı" dedi, dosyada on üç tur vardı.** `02-borclar.md:3-177`
   içinde adsız *"Sayım yöntemi"* + *ikinci*–*on üçüncü* = **13 ölçüm turu**, toplam
   **27 italik blok** (ölçümler + `Üretilen komutlar` kayıtları + yöntem düzeltmeleri).
   Ölçüm: `sed -n '3,177p'` üzerine `awk` blok sayımı ve `grep -oE` ile sıra-adı
   tekilleştirmesi. Kesim brief'in tarifine göre değil **dosyanın gerçeğine göre** yapıldı.

### MailerLite alan sayısı bilerek üretilene gitmedi

Brief bu satırı *"alan sayısı üretilene"* diye bölüyordu. **Ölçüt kendi kendini çürüttü:**
bu repodan hiçbir komut MailerLite hesabının alan listesini yeniden üretemez —
`grep -rn "custom_fields\|fields:" src/lib src/pages/api` üç eşleşme veriyor, üçü de
alanları çalışma anında kuruyor, sabit liste yok; panel CC'ye kapalı (`05-harita.md` §3).
KARAR 578'in ölçütüne göre satır **yazılandır**. Uydurma bir üretilen alan açmak yerine
satır yerinde bırakıldı, gerekçesi yazıldı ve eksik **B194** olarak deftere geçti.
Brief'in tarifi ile kararın ölçütü çeliştiğinde **ölçüt kazandı.**

### Denetçi kendi ilk koşumunda daraltıldı

`baslik-denetim.mjs`'in ölçüm-paragrafı deseni brief'te `^\*.*ölçüm` yazıyordu. İlk koşum
`00-durum.md:3`'ü yakaladı — ama o satır `**Son güncelleme:** … **ölçüm / yargı ayrımı
turu**`, yani **tur adının kendisi**. Desen `^(⚠\s*)?\*(?!\*).*ölçüm` oldu: tek yıldızla
açılan italik blok, kalınla açılan satır değil. CLAUDE.md §3'ün kuralı: *aranan dizenin
korunması gereken metinde de geçip geçmediği kontrol edilmeden kriter yazılmaz.*

### Build sayımı: iki yöntem, iki rakam, tek tanım

| | eski (elle) | yeni (betik) |
|---|---|---|
| prerender | 32 | **41** |
| SSR (sayfa) | 10 | **12** |
| API route | 6 | **6** |

Fark **tanım farkıdır, gerileme değil** (KARAR 470: *iki ölçüm çelişirse önce tanımlar
karşılaştırılır*). Betiğin tanımı: prerender = `dist/client` altındaki `index.html` sayısı ·
SSR = `.vercel/output/config.json`'da `dest:"_render"` (`_image`/`_server-islands` hariç).
Bu tanım `/istanbul/*` ve `/workshop/*` yönlendirme takma adlarını **sayar**; elle tutulan
eski sayım saymıyordu. API ayağı iki yöntemde de aynı çıktı — tanımın ayrışmadığı tek yer.
Rakam artık tanımıyla birlikte `04-olcum.md`'de yazılı.

### `00-durum.md` satır sayısı: 197 → 199

Tur dosyayı **kısaltmadı.** Üç satır işaretçiye indi, başlıktan iki blok çıktı; ama
yerlerine KARAR 578 kural notu ve yeni yönlendirme satırı geldi. Tavan (**≤200**,
KARAR 457) altında ama **payı iki satır** — sonraki tahliye kaçınılmaz ve onu erteleyen
her tur cap'e daha yakın başlar.

### `00-durum.md` başlığından inen dönem ayrıntısı

Dönem HEAD satırı bu turda yenilendi; 10 Eylül döneminin deploy kaydı silinmesin diye
buraya iniyor (KARAR 61):

> *10 Eyl'de beş production deploy'u alındı: `e03364d · 213ea0b · 4eb549b · b1ee821 ·
> b1ee821` (sonuncusu redeploy). Production 26 Ağu'dan 10 Eyl'e kadar `7062846`
> üstündeydi — 15 gün, `9acbabb` değil; önceki dönem satırı dönem HEAD'ini deploy
> sanıyordu (KARAR 577'nin doğuş vakalarından).*

### B179: bir ayak kapandı, madde açık

Kaan `vercel link` koşmuş — `.vercel/repo.json` tazelendi ve artık projeye **`ocak`**
diyor, *"ocak-site"* kalıntısı gitti. **Ad ayağı ✅.** Dosya silinmedi, gereği de kalmadı.
`project.json` hâlâ yok, yani `vercel --prod` yolu hâlâ kurulu değil — **madde damgasız
kalıyor.** Kapsam daraltmak borcu kapatmaz; B19 ve B57'de ölçülen aynı kural.

### AÇILAN BORÇLAR

**B194** (MailerLite alan sayısı hiçbir komutla üretilemiyor — KARAR 578'in kapatamadığı
uç) · **B195** (`baslik-denetim.mjs`'i koşan mekanizma yok; betiğin bozulma biçimi tam da
koşulmamaktır ve çıktısı sessizlik olduğu için görünmez).

**GÜNCELLENEN:** **B179** (ad ayağı ✅, `project.json` ayağı açık).
**KAPANAN:** yok.

### Beş commit

| # | hash | konu |
|---|---|---|
| 1 | `3828f35` | `scripts/durum-uret.mjs` + `docs/04-olcum.md` ilk koşum |
| 2 | `7047cc5` | `00-durum.md` bölünmesi — üretilen işaretçiye, yargı yerinde |
| 3 | `18a0289` | `scripts/baslik-denetim.mjs` doğdu |
| 4 | `ed2f2d6` | başlık tarihçeleri kronolojiye indi (tek başına geri alınabilir) |
| 5 | *(kapanış)* | ledger + harita + skill + `CLAUDE.md` + bu kayıt |

**Dönem HEAD `ed2f2d6`** — kapanış commit'inden bir önceki (KARAR 474).

---

## 2026-09-11 — kapanış turu · KARAR 583 · B195 kapandı, açılan yok · HEAD de74216

⚠ **Kaynak beyanı:** bu turdaki **B64 ölçümünün** kaynağı bir **Claude.ai Vercel MCP
oturumudur** (11 Eylül). Panel yüzeyi CC'ye kapalıdır (`05-harita.md` §3); CC ölçümü
doğrulamadı, `vercel` CLI çağırmadı. Ölçüm **teyitsiz** olarak işaretlidir — tek
istisna, yerel `git branch -v` ile çakıştırılabilen commit ayağıdır (`7bf265b`).

Zemin `de74216` — gece turunun kapanış commit'i. Dört iş: push · B195 kapanışı ·
B64 ölçümünün işlenmesi · B179 damga teyidi. KARAR 103 kipi, otonom koşuldu.

### İŞ 1 — push edildi, ve gece turunun gerekçesi düzeltildi

`dc74632..de74216` → `origin/main`. Beş commit yayında.

⚠ **Gece turu push'u atlamıştı ve gerekçesi fazla genişti.** *"main = production,
push deploy tetikler"* doğru bir cümle, ama bu repoda **doküman commit'i de production'a
deploy oluyor** ve bu yeni bir şey değil: son üç production deploy'unun üçü de docs
commit'idir (`dc74632` · `af2cda2` · `b1ee821`). Brief'in *"deploy alınmaz"* satırı
**promote/eyeball turunu** kastediyordu, push'u değil. Notion'a gece dokunulmadı, yani
rebuild içerik-aynı.

**Ders:** doğru bir kuralı fazla geniş uygulamak da bir sapmadır. *"Riskli olabilir"*
ile *"bu turda riskli"* ayrı iddialardır; ikincisi ölçülür, birincisi varsayılır.
Gece turu birincisini yazdı ve işi yarım bıraktı.

### İŞ 2 — B195 ✅ kapandı (açıldığı gün)

- **KARAR 583 — BAŞLIK DENETÇİSİ `ocak-arsivci`'NİN ADIM 0'INDA KOŞAR (AKTIF):**
  `node scripts/baslik-denetim.mjs`, çıkış kodu 0 değilse **DUR**. Aynı skill'in DUR
  listesinde 9. madde olarak da duruyor; `CLAUDE.md` §4(c) yeri adıyla işaret ediyor.

**İki alternatif reddedildi, gerekçeleriyle:** *pre-commit hook* her **kod** commit'inde
boşuna ateşler — denetlediği yüzeye hiç dokunmayan commit'lerde de koşar, ve boşuna
ateşleyen bir kural aşınır. *`npm run denetim` girdisi* **hatırlamak gerektirir**, yani
borcun kendisini — koşulmama riskini — çözmez, yalnız adını değiştirir.

**Neden ADIM 0 doğru kapı:** başlık şişmesi yalnız **patch yazımında** doğar, kod
commit'inde değil. Denetçi böylece riskin doğduğu anda, zaten zorunlu olan tek kapıda
koşuyor — ek hatırlama yükü getirmeden. Bir gün ömürlü borç: açıldığı turda görüldü,
sonraki turda kapandı.

### İŞ 3 — B64 tahminden ölçüme geçti; madde KAPANMADI

Ölçüm **Claude.ai'nin Vercel MCP'sinden** geldi; CC'ye o yüzey kapalıdır
(`05-harita.md` §3) ve CC doğrulamaya çalışmadı, `vercel` CLI çağırmadı.

| alan | değer |
|---|---|
| hook | `notion-content-update` (`tZR9LcwJq9`) |
| `ref` | `astro-iskelet` |
| üretilen commit | `7bf265b` — sekiz ardışık koşumda **hiç değişmedi** |
| `target` | `null` → **preview**, production değil |
| ritim | günlük, ~24 saat · son koşum 11 Eylül |
| sapma | `astro-iskelet`, `main`'in **163** commit gerisinde |

**Teşhis değişti.** Defterdeki belirti *"hook oluşturuldu ama çağıran yok"*du; ölçüm
bunun yarısını çürüttü: **eski hook çağrılıyor ve her gece koşuyor.** Arıza çağrılmamak
değil, **yanlış yere basmak** — ölü dalı preview olarak yeniden inşa ediyor, production'a
hiç değmiyor. Sekiz koşumda commit'in sabit kalması bunun kanıtıdır: dal ilerlemiyor,
build her gece aynı ağacı derliyor. Çalışmayan bir hook gürültü çıkarırdı; bu sessiz —
ve sessiz arıza, `00-durum.md`'nin *"sessiz kırılma noktaları"* sınıfının tanımıdır.

**Yerel teyit tek noktada var ve tuttu:** `git branch -v` yerel `astro-iskelet`'i
`7bf265b`'de gösteriyor — ölçümün verdiği commit ile aynı. Kapalı yüzeyden gelen bir
rakamın, açık yüzeyden görünen ayağıyla çakıştırılabildiği yer burasıydı ve çakıştı.
Geri kalan alanlar (`target` · ritim · koşum sayısı) yalnız panelden görünür ve
**teyitsizdir**; blok bunu adıyla söylüyor.

`00-durum.md`'nin `Deploy hook` satırı bu rakamlarla yeniden yazıldı — **yazılan sınıf**
(KARAR 578): kaynağı CC'ye kapalı bir yüzey, hiçbir komut üretemez, dolayısıyla
`04-olcum.md`'ye giremez. Satır kaynağını ve tarihini taşıyor, yoksa sessizce bayatlar.

⚠ **KARAR açılmadı** — bu bir ölçümdür, karar değil. B64'ün çözümü ayrı tur.

B64 gövdesindeki *"85 commit geride"* rakamı **silinmedi**: yazıldığı gün doğruydu,
altına 163 notu düşüldü. Borcun ne zaman ne kadar büyüdüğü bilgisi işe yarıyor (KARAR 61).

### İŞ 4 — B179 damgası teyit edildi, madde açık

Gece turu gövdeye iki ayağı da yazmıştı; bu turda **doğrulandı, tekrarlanmadı**:
başlıkta damga **yok** (`grep -E '^## B179' | grep -cE '[✅❌]'` → 0), gövdede ad ayağı
✅ ve `project.json` ayağı ⏳ ayrı satırlarda duruyor. Madde **açık** sayılıyor.
Kapsam daraltmak borcu kapatmaz — B19 ve B57'de ölçülen aynı kural.

### Turun ders satırı

Bu turun üç işi de aynı sınıftandı: **bir önceki turun bıraktığı yarım iddiayı ölçüyle
kapatmak.** Gece turu push'u varsayımla atladı, B64'ü tahminle yazdı, B195'i *"ayrı
karar"* diye erteledi. Üçü de bir tur sonra, ölçüm ya da karar geldiğinde on dakikada
kapandı. **Erteleme bedelsiz değildir; bedeli bir sonraki turun ADIM 0'ıdır.**

---

## 2026-09-11 — dal temizliği + üç kapanış turu · KARAR 584 · B64 · B73 · B179 kapandı, açılan yok · HEAD d8732c4

Zemin `d8732c4`. Beş iş, **sıfırı kod** — `src/` ve `public/` açılmadı; tek kod dokunuşu
`scripts/durum-uret.mjs`'in bir çıktı satırıdır. KARAR 103 kipi: kararlar Claude.ai'de
mühürlendi, CC otonom koştu.

⚠ **Kaynak beyanı — bu turun iki işi kapalı yüzeyden geldi.** B64'ün kapanış ölçümü
(n8n · Notion automation · Vercel deployment alanları) ve B179'un `vercel link` denemeleri
**Kaan'ın ve bir Claude.ai Vercel MCP oturumunun beyanıdır** (`05-harita.md` §3). CC ne
`vercel` CLI çağırdı ne n8n'e baktı. Yerelde çakıştırılabilen ayaklar ayrıca ölçüldü ve
aşağıda adıyla yazılı.

### Turun tek cümlesi

Üç borç aynı gün kapandı ama **üç farklı yoldan**: B64 *düzeltilerek*, B73 *cevaplanarak*,
B179 *kabul edilerek*. Üçüncü yolun ledger'da adı yok — bir borcun sapması ölçülüp
zararsız bulunduğunda hangi `durum` değerini aldığı tanımsız. Bu turun açtığı asıl soru bu.

### İŞ 1 — B64 ✅ kapandı; dört aylık ikinci teşhis de onunla kapandı

Kapanış şartının dört ayağının dördü karşılandı ve **üçüncü bir çağıran** ortaya çıktı:
n8n *"OCAK Gecelik Rebuild"* akışı da yeni hook'a bağlandı.

| ayak | değer |
|---|---|
| yeni hook | `notion-content-update-main` (`x2LnNpVvuG`), ref `main` |
| n8n elle tetikleme | deployment **READY 52 sn** · `githubCommitRef: main` · `target: production` · commit `d8732c4` · alias'ta `www.ocak.biz` |
| Notion automation | `/hikaye` Yayınla uncheck→check → ~30 sn'de production build, `deployHookName: notion-content-update-main` |
| eski hook | `tZR9LcwJq9` **revoke edildi** |

**İkinci teşhis de burada kapandı.** Mayıs'tan beri *"Notion automation'ı bozuk"* diye
taşınan belirtinin sebebi ayrı bir arıza değildi: automation **çalışıyordu**, ölü hook'a
basıyordu. Dört ay boyunca iki ayrı arıza sanılan şey tek arızaydı — ve teşhis, ancak
gerçek ayar panelde ölçüldüğünde ayrıştı. Bir önceki turun dersi (*"tahminle yazılan
belirti bir turu yer"*) burada dört aylık versiyonuyla çıktı.

**KARAR 485** `ACIK-BORC` → **AKTIF.** Yeni karar açılmadı, bilerek: uygulanan şey
19 Ağustos'ta zaten mühürlenmiş karardı — borç, kararın *uygulanmamış olmasıydı*.

### İŞ 2 — B73 ✅ kapandı; cevap "hayır"

`git ls-remote --tags origin` **tek** tag döndürdü (`kurtarma-2026-08-19`);
`pre-merge-kayit-penceresi` uzağa **hiç push edilmemiş.** Temmuz'dan beri açık duran soru
cevaplandı: gönderilmeyecek. Kurtarma çivisi yerel bir güvenlik ağıdır, paylaşılan bir
işaret değil.

⚠ **Kapanış şartı daraltıldı ve daraltma yazıldı.** Şart *"gönderilmeyecekse yerelden
silinir"* diyordu; tag'e dokunulmadı. Ölçüm daraltmayı ucuzlattı ama iptal etmedi:
`pre-merge-kayit-penceresi` = `3c2b865` ve `merge-base --is-ancestor` **0 döndü** — tag
main'in atası, işaret ettiği ağaç zaten `main` üzerinden erişilebilir, tutmak da silmek de
veri kaybı üretmiyor. Yani aynı turda **silinen** `kurtarma-2026-08-19` ile aynı sınıfta;
ayrım teknik değil, tercih.

### İŞ 3 — dal temizliği; dört dal + bir tag silindi, biri bilerek bırakıldı

Ölçüt tekti ve dördünde de aynı çıktı: `git log main..origin/<dal>` **boş** —
main'de olmayan tek bir commit taşımıyorlardı.

**Silinen (uzak):** `astro-iskelet` · `davet-mail-baglam` · `etkinlik-kayit-penceresi` ·
`liste-ailesi` · tag `kurtarma-2026-08-19` (`688bee5`, main'in atası olduğu
`merge-base --is-ancestor` ile doğrulandı). Yerel `astro-iskelet` `git branch -d` ile
düştü — `-d` bilerek seçildi: birleşmemiş olsaydı reddedecekti, yani silme kararı
ölçümü kendi içinde taşıyordu.

**Kalan uzak dallar: `main` + `nkolay-test`.** İkincisi **bilerek duruyor** — ödeme Preview
ortamı ve `ODEME_CALLBACK_SIR`'ı production'dan ayrı (KARAR 575). `03-sira.md`'nin
*"iş bitince sil"* maddesi tek başına yanıltıcıydı; şart oraya yazıldı.

Yeni çivi: `kurtarma-2026-09-11-b64-dal-temizligi` (yerel, `d8732c4`).

⚠ Bu iş **yeni borç açmadı.** Silinen dalların hiçbiri bir kayıt taşımıyordu; kayıt
değeri olan tek şey silme kararının gerekçesiydi ve o burada.

### İŞ 4 — B179 ✅ kapandı; cevap "kabul edildi"

- **KARAR 584 — VERCEL KİMLİĞİ `repo.json`'DAN OKUNUR (AKTIF):** `.vercel/project.json`
  aranmaz. `rm -rf .vercel && vercel link --yes` koşuldu; CLI yine yalnız `repo.json`
  yazdı. Üç deneme aynı sonucu verdi — yani bayat bir dosyanın kalıntısı değil, CLI'ın
  şu anki davranışı. Sebep: proje GitHub'a bağlı olduğu için CLI bağlamayı **repo
  seviyesinde** kuruyor; `repo.json` o bağlamanın doğru ve tam çıktısıdır. Eksik olan bir
  dosya değil, **başka bir bağlama modeli.** `project.json`'a bağımlı tek yol
  `vercel --prod` ve kullanılmıyor — deploy git push'la gidiyor. `scripts/durum-uret.mjs`
  kimliği zaten `repo.json`'dan okuyor ve kaynağını `04-olcum.md`'ye yazıyor.
  `←579 · ←567 · ↔B179`

⚠ **Bu, "kabul edildi" sınıfının ilk vakasıdır** ve kapanış yolu ledger'da tanımlı değil.
Borç ne düzeltilerek ne iptal edilerek kapandı: **sapması ölçüldü, zararsız bulundu ve
doktrine döndü.** Dokuz `durum` değerinin hiçbiri bunu adıyla tarif etmiyor — `AKTIF` bir
kararı anlatır, `IPTAL` vazgeçişi, `KULLANILMADI` kullanılmamayı. Not `03-sira.md`'ye
düşüldü; tanım ayrı tur.

⚠ **Ayrım ince ve korunmalı:** *"kabul edildi"* **ölçülmüş bir zararsızlıktır**, kapsam
daraltması değil. B19 · B57 · B73'te reddedilen şey ölçülmeden daraltmaktı — burada
ölçüm önce geldi, üç kez tekrarlandı ve sapmanın bağımlı olduğu tek yolun kullanılmadığı
gösterildi. İki şey aynı kelimeyle anlatılırsa, gelecekte her daraltma "kabul" diye geçer.

`scripts/durum-uret.mjs`'in Vercel bloğu bu kararla hizalandı: çıktı artık `project.json`
yokluğunu **açık borç** diye değil, **beklenen hâl** diye bildiriyor ve KARAR 584'e
işaret ediyor. Üretilen dosya elle düzeltilmez; betik düzeltildi (`CLAUDE.md` §4c).

### İŞ 5 — `00-durum.md` üç satırı yeniden yazıldı

`Dal modeli` (artık `main` + `nkolay-test`, commit farkı yok — o `04-olcum.md`'nin işi) ·
`Deploy hook` (B64 kapanışı; eski ölçüm bloğu **silinmedi**, `02-borclar.md` ve bu dosyaya
taşındı — KARAR 61) · `Vercel` (B179 kapanışı + KARAR 584).

⚠ `Deploy hook` hücresi bir önceki turda **bayt olarak şişmişti** (`03-sira.md` madde 1).
Bu turda kapanış onu kendiliğinden küçülttü: ölçüm tarihçesi kendi dosyalarına inince
hücrede yalnız güncel gerçek kaldı. Tavanın **yanlış birimi ölçtüğü** tespiti geçerliliğini
koruyor — bu turda sorun *çözülmedi*, konu kapandığı için *görünmedi*.

### Turun ders satırı

Bir borcun kapanışı, borcun kendisinden fazlasını kapatabilir. B64 dört aylık ikinci bir
teşhisi de beraberinde kapattı; B179 kapanırken ledger'da **tanımsız bir kapanış yolu**
olduğunu görünür kıldı. **Kapanış, en ucuz teşhis anıdır** — çünkü o an, açık kalan her
şeyin gerekçesi bir kez daha okunur.

**Dönem HEAD `d8732c4`** — kapanış commit'inden bir önceki (KARAR 474).

---

## 2026-09-11 — kapanış turu ikinci geçiş: sözleşmesiz dosya ölçümü · KARAR açılmadı · B39 güncellendi, açılan/kapanan yok · HEAD 094fd0f

Zemin `094fd0f`. Aynı günün kapanış brief'i **altı işli** bir sürümle yeniden geldi;
beşi zaten koşulmuştu (`094fd0f`), yeni olan tek iş **İŞ 5 — sözleşmesiz dosyalar**dı.
Ve o iş **koşulmadı: ölçüm brief'i çürüttü.**

### Brief bir borç açılmasını istedi, ölçüm mükerrer olduğunu gösterdi

Brief *"beş dosyanın sözleşmesi yok, borç açılır"* diyordu. Ölçüm beşi doğruladı —
`docs/` kökündeki `.md` dosyalarından `05-harita.md` §2'de adı geçmeyenler, `20-ref-*.md`
globu ve `05-harita.md`'nin kendi §4'ü elenerek: `31-zemin.md` · `ocak-kaynak-kanonu.md` ·
`Ocak-Mufredat.md` · `sayfa-yazim-rehberi.md` · `2026-08-06-ocak-gecis-plani.md`.

**Ama beşin üçü zaten kayıtlıydı.** `B39` (8 Ağustos) ilk ikisini gövdesinde taşıyor —
447 satır evsiz içerik, %95,4 örtüşmesiz — ve üçüncüsünü *"İşaretlenen, kapsam dışı"*
satırında adıyla anıyor. Geçiş planının sapma kaydı üçünü de sayıyor (madde 6 · 6-ek · 9).
Yeni bir `B196` **mükerrer olurdu**: aynı eksikliği ikinci kez, farklı numarayla defterlemek.

Kayıtsız kalan tek dosya **`31-zemin.md`**'dir — numaralı ve kanonik, yani adlandırma
şemasının *içinde* ama sözleşmenin *dışında*. B39'un kapsamı iki içerik dosyasıdır, bunu
kapsamıyor. Borç açmak yerine **B39 gövdesine not düşüldü**; sözleşme metni bir yargıdır
ve Claude.ai'ye aittir, ayrı tur.

**Aynı nota ikinci ayak — kendi kuralını denetlemeyen denetçi.** `05-harita.md:3` kendi
başlığında `önceki: 11 Ağustos 2026 · B47` taşıyor. KARAR 581 `önceki:` zincirini
başlıktan kaldırmıştı ve `scripts/baslik-denetim.mjs` bu deseni arıyor (`ONCEKI_DESENI`),
ama `DOSYALAR` listesi **üç** dosyadır — harita o üçte değil. Yani kural ihlali duruyor
**ve ihlali gösterecek araç ona bakmıyor.** B195'in kapattığı körlüğün aynısı, bir yüzey
ötede: denetçinin kendi kapsamı denetlenmiyor.

### Turun ders satırı

**Borç açmadan önce defteri ara.** Bu tur bir borç açmakla değil, **açmayarak** kapandı —
ve kazanç, ölçümün brief'ten önce koşulmasıydı (KARAR 102). Bir eksikliğin *görülmesi*
onun *kayıtsız* olduğunu göstermez; ikisi ayrı iddiadır ve ikincisi aranarak ölçülür.
Mükerrer borç, kapanmayan borçtan daha pahalıdır: ikisi de açık görünür, biri sahtedir.

**Dönem HEAD `094fd0f`** — kapanış commit'inden bir önceki (KARAR 474).

---

## `00-durum.md`'DEN TAHLİYE — 19 AĞUSTOS'UN İKİ BLOĞU (11 Eylül 2026)

İkisi de `BU DÖNEM NE OLDU` bölümünün en eski kayıtlarıydı ve aynı tarihi taşıyorlardı.
WA ikinci hat turu `YAYINI KİLİTLEYENLER` madde 1'i iki satırdan dokuz satıra çıkardı;
KARAR 457 tavanı (200) yer açılmadan tutmuyordu. **İçerik silinmedi, kırpılmadı,
özetlenmedi — olduğu gibi taşındı** (KARAR 61 · 88). Tekil blok tahliyesi dosyayı 199'da
bırakıyordu, yani tavana yapışık; iki blok birlikte indi ki sonraki tur de sığsın.

- **19 Ağustos (B turu — sosyal medya + AÇILIŞ):** Sosyal v2 planı taze site dumpına karşı
  ölçüldü — **27 aynen · 3 cümle düzeltmesi · 0 yeniden yazım · 5 yeni malzeme**; baştan yazım
  düştü, v2.1 patch'lendi ve `30-sosyal.md` olarak repoya alındı. **AÇILIŞ 24–27 Eylül 2026**
  kesinleşti (KARAR 492). Dump 2. turda kabul testinden geçti; 1. tur etkinlik gövdelerinin
  dörtte üçünü sessizce kaybetmişti — KARAR 495 bu vakadan doğdu. → `90-kronoloji/2026-08.md`
- **19 Ağustos (Faz 1 — ödeme yüzeyi):** Kart akışı **silinmeden** kapatıldı
  (`KART_AKISI`, KARAR 488). Referans kodu `OCAK-XXXX`'e indi — 29'luk alfabe,
  15 maddelik kara liste (KARAR 489 · 490). Havale açıklamasından **isim çıktı**,
  satır saf ASCII oldu. Forma **Soyad** eklendi, **Şehir** ve **Telefon** sunucuda
  zorunlulaştı; `last_name` MailerLite'a canlı aboneden teyitli (**B76 ✅**).
  `A.Ş.` satır kırılması CSS'ti, düzeldi. Geçersiz alan kaydırması bir kez kör
  uygulanıp canlıda kapanmadı, `nav-kaydir.ts` ortak yardımcısıyla düzeldi
  (KARAR 491 bu hatadan doğdu). → `90-kronoloji/2026-08.md`

---

## 2026-09-11 — WA ikinci hat turu · KARAR 585–590 · B196–B199 açıldı, B19·B104 kapandı · HEAD b5ff542

**Yön değişikliği.** Tur, `…0888`'i WABA'dan *kaldırma* niyetiyle açıldı (numara telefondaki
WhatsApp Business'a kurulmuştu, Kanal ve `ocak.biz` kullanıcı adı oradaydı). Meta'nın
"Ocak Kadın Topluluğu" adını onaylaması yönü tersine çevirdi: numara bota bağlanacak, Kanal
feda edilecek. Kaan kararı bilerek verdi. B104'ün *"site kodu değişmez"* varsayımı da aynı anda
düştü — site bot hattını `…0888`'e çevirdi.

**Gecenin teşhis hatası — dört saat, tek karakter.** `register` çağrısı ve ardından gelen her
WABA sorgusu (#200) *Provide valid app ID* verdi. Sırayla şunlar suçlandı ve hepsi yanlış
çıktı: token izinleri, system user ataması, uygulamanın silinmiş olması, portföyde olmayan bir
WABA, Meta yayılma gecikmesi. Gerçek sebep Claude.ai'nin verdiği n8n ifade yazımıydı:
`=Bearer {{$env.X}}` — URL alanına elle `=` konması ve `{{` sonrası boşluğun düşmesi değişkeni
boş gönderiyordu. Canlı *WA Gönder* düğümünün kendi yazımı (`Bearer {{ $env.X }}`) kopyalanınca
çağrı ilk denemede yeşil döndü. Ders KARAR 587'de mühürlendi. İkinci ders: ölçüm aracını uzak
tarafa kurarken **aracın kendi sözdizimi de bir değişkendir.**

**Yan hasar.** Teşhis sırasında `ocak-bot` system user'ının token'ları revoke edildi → bot bir
süre sustu (webhook'a mesaj düşmüyordu). Yeni token üretildikten ve WABA ataması yayıldıktan
sonra kendiliğinden ayağa kalktı. Ayrıca **bir token ekran görüntüsünde açığa çıktı ve revoke
edildi** — Claude.ai'nin önerdiği ölçüm token'ı URL sorgu dizesine koyduruyordu, hata mesajı da
URL'yi olduğu gibi bastı. **Token her zaman başlıkta kalır.**

**Aynı adın iki kaydı.** `…0888` iki WABA'da birden göründü; ikisinin de adı
"Ocak · Dijital Asistan" değildi ama biri öyle görünüyordu ve bir ara üçüncü bir WABA ID'si
(`4623995187880191`, geliştirici panelinde) daha belirdi. Ad üzerinden akıl yürütmek iki kez
yanlış teşhise yol açtı. KARAR 588.

**Kod ayağı — `b5ff542`.** ADIM 0 sapma bulmadı: kod tarafında tek canlı değer (`api.ts`), dört
yorum, kullanıcıya görünen metin sıfır. Brief'in `208 08 88` grep deseni sitedeki `208 0888`
yazımıyla tutmadı — CC bunu bilgi olarak raporladı, iş durmadı; **desen yazımı da bir ölçüm
hatasıdır** (aynı sınıf bu turda iki kez tekrarlandı: Vercel durum deseni de aracın gerçek
çıktısına değil beklentiye göre yazılmıştı — `vercel inspect` `● Ready` basar, `READY` değil).
Doc yorumu silinmedi, **dönüştürüldü** (KARAR 61): eski iki-yüzey ayrımı tarihçe olarak duruyor,
ama literal `905325555226` dizesi düşürüldü — *"sitede hiç kalmayacak"* kriteri böyle korundu.
Canlı ölçüm: `/` → 2 link, `/iletisim` → 3, eski numara 0.

**Notion.** `/sss` *"Telefonla aranabilir miyim?"* cevabına, yasal sayfadaki numaranın arama
almadığını söyleyen cümle eklendi (Kaan, elle — KARAR 459).

### Turun ders satırı

**Bir borç kapanış şartıyla kapanır, varsayımıyla değil.** B104 *"site kodu değişmez"* diyordu
ve kapanış şartı canlı round-trip'ti. Yön değişince varsayım düştü, şart ayakta kaldı — borç
şartından kapandı. Şartı gövdesindeki varsayıma bağlayan bir borç, yön her değiştiğinde
kapanamaz hâle gelirdi.

**Dönem HEAD `b5ff542`** — kapanış commit'inden bir önceki (KARAR 474).

### Mühürlenen kararlar

- **KARAR 585 — BOT HATTI İLE YASAL SAYFA TELEFONU AYNI NUMARADA BİRLEŞTİ (AKTIF):**
  `WHATSAPP_URL` → `905322080888`. İki yüzeyi ayrı tutma kuralı (KARAR 518) sona erdi;
  518 **SUPERSEDE → 585**. Beş yasal sayfa zaten bu numarayı taşıyordu ve dokunulmadı —
  hizalanma kendiliğinden oldu. Bundan sonra **ayırmaya** çalışan tur yanılır.
- **KARAR 586 — İKİ WHATSAPP HATTI TEK n8n WORKFLOW'UNDA YAŞAR (KALICI):** Filtre düğümü
  pnid'yi gelen payload'ın `metadata.phone_number_id`'sinden türetir, cevap yazılan hattan
  gider. Numara geçişi n8n'de **sıfır değişiklik** gerektirdi — hattı workflow değil, payload
  belirliyor.
- **KARAR 587 — n8n İFADE YAZIMI TEK BİÇİMDİR (KALICI):** `Bearer {{ $env.X }}`. URL alanına
  elle `=` yazılmaz — ifade modunu n8n kendi açar. Yanlış yazım değişkeni **boş** gönderir ve
  hata **uzak tarafı** işaret eder; teşhis yanlış yöne gider. Bu turda dört saat maliyeti oldu.
- **KARAR 588 — META VARLIK KİMLİĞİ ÜÇ KATMANDIR (KALICI):** Business Portfolio ↔ WABA ↔
  phone number ID. Üçü de "ID" diye anılır ve **aynı adı taşıyan iki WABA mümkündür** —
  ayrım ad ile değil **ID ile** yapılır. Ad üzerinden akıl yürütmek bu turda iki kez yanlış
  teşhise yol açtı.
- **KARAR 589 — BİR NUMARA YA CLOUD API'DE YA TÜKETİCİ UYGULAMASINDA YAŞAR (KALICI):**
  register, o numaradaki **Kanal, kullanıcı adı ve sohbet geçmişini geri dönüşsüz düşürür.**
  İkisi aynı anda tutulamaz. Bedeli bilerek ödendi (B197).
- **KARAR 590 — YASAL SAYFADAKİ TELEFON BİR SES HATTI DEĞİLDİR (AKTIF):** ve bu SSS'de
  **açıkça söylenir.** Kaan'ın kişisel numarası yasal sayfaya yazılmaz — geri dönüşü zor bir
  açılma, ve dışa konuşan yüz Advaita olduğu için devirde numara değişmek zorunda kalır.
  Kalıcı yol yönlendirmeli sanal numara (B196).

## 2026-09-11 — N-Kolay sağlayıcı turu · KARAR 591–596 · B200–B203 açıldı · B186 kapandı · HEAD 5ace816

**Açılış durumu:** kart akışı Production'da açık ama **mock** (B193 yürürlükte).
`payment-provider.ts` iki metotlu arayüz + factory; `dogrulaCallback()` var, mock
implementasyonu paylaşılan sır karşılaştırıyor, fail-closed. N-Kolay ile anlaşıldı,
entegrasyon dokümanı okundu (`paynkolay.com.tr/entegrasyon/`, beş sayfa).

**Akışın tipi ölçüldü:** Ortak Ödeme Sayfası — **form POST redirect**, API-key'li JSON
çağrısı değil. Kart bilgisi doğrudan N-Kolay'a gider. Test `paynkolaytest.nkolayislem.com.tr/Vpos`,
prod `paynkolay.nkolayislem.com.tr/Vpos`. Dönüş bildirimi **tarayıcıdan** gelir — sunucu-sunucu
webhook yoktur (bu, B200'ün doğduğu yer).

**Dört tur, yedi commit:**

1. `ef09c47` · `443e45a` — **origin muhafızı.** Astro `checkOrigin` kapatıldı, yerine
   `src/lib/origin-muhafiz.ts` + üç iç route (`kayit` · `form` · `promo-dogrula`); kural
   `src/lib/origin-kural.ts`'de **tek nüsha** (ikinci commit taşıma, 32 satırın 31'i bayt
   bayt aynı, tek fark dönüş tipinin adı). `/api/davet` **bilerek dışarıda** — orada bilinçli
   sessiz 200 var, üstüne 403 koymak o kararı ölçmeden geri alırdı.
   Canlı doğrulama (`www.ocak.biz/api/promo-dogrula`): kendi origin → 400 · yabancı → 403 ·
   Origin yok → 403 · Origin yok + kendi Referer → 400 · 403 gövdesi sebep sızdırmıyor.
2. `54599ea` — **`Beklenen Tutar` boru hattı.** `notionKayitlaraYaz` alanı yazar,
   `kayitOku()` taşır. Ölçüm bedava değildi: Kayıtlar DB'sinde `OLCUM-KURUS-<epoch>` satırı
   açıldı, okundu, arşivlendi — **Notion `number` kuruşu koruyor** (937.5 · 1234.56 · 0.01
   üçü de birebir döndü). `amount` `toFixed(2)` ile gider; `toLocaleString('tr-TR')` virgül
   basar ve imzayı **sessizce** bozardı, test bunu çiviliyor.
3. `b53802a` — **N-Kolay sağlayıcısı.** `/odeme/nkolay` sayfası kaydı Notion'dan okur,
   tutarı **oradan** alır (query'den asla), `cardHolderIP`'i `x-forwarded-for`'un ilk
   değerinden kurar, on alanlı formu auto-submit eder. `rnd` **istek anında** üretilir
   (build-time değil, KARAR 385/464 vakası) ve hash'e giren dize ile body'deki dize tek
   değişkenden gelir.
4. `12739d3` · `3dc73dd` — **B186 kapandı.** 21 satır dönüştürüldü, iki muhafız korundu.
   Build çıktısında `iyzico|paytr` **7 → 0** (`.vercel/output/functions`).
5. `5ace816` — **tutar + replay kapıları.** vitest 325 → **398**.

**Ölçümün brief'i çürüttüğü dört yer** — hepsinde ölçüm kazandı (KARAR 102):

- Brief *"KART_AKISI kapalıyken 410"* dedi; `/odeme/mock` **404** dönüyor. 410 webhook
  desenidir (`/api/odeme-callback`), 404 sayfa desenidir — **bilerek ayrı.** 404 yazıldı.
- Brief *"`src/lib` → `src/pages` bağımlılığı"* dedi; öyle bir bağımlılık **hiç yoktu**.
  Kapatılan yön `lib → lib` (genel → özel) idi.
- Brief B186'yı **19 satır** saydı; ölçüm **23** dedi, temizlenen **21** oldu (aradaki fark
  bu turun kendi eklediği ikinci muhafız). Eski rakam **silinmedi**, altına düzeltme kondu.
  Borcun ikinci iddiası da yanlıştı: throw'lar `kayit.astro.mjs`'de değil
  `chunks/payment-provider_*.mjs`'de yaşıyordu.
- Brief *"kaydı `CLIENT_REFERENCE_CODE`'dan çöz"* dedi ve **kendi içinde çelişti** — aynı
  brief *"yalnız hash kapsamındaki alandan"* diyordu. `CLIENT_REFERENCE_CODE` hash
  kapsamında **değil** ve repoda hiç geçmiyor. Çelişki KARAR 593 ile kapandı.

**İki bilinçli sapma (CC, raporlu):** redirect `?ref=` değil `?ref=…&kodId=…` — `kodId`
taşınmazsa `kodKullanimArtir` hiç çalışmaz, promo sayacı sessizce ölürdü. Ve
`KayitOkumaSonuc`'a `tutar` yanında `pageId` de eklendi — yoksa callback `pages.update`'i
`OCAK-XXXX` ile dener ve ödeme alınır, kayıt `Beklemede` kalır.

**Denetim kriter listesi bulundu** — `paynkolay.com.tr/sss`, *Başvuru* bölümü,
*"Başvuru için gereken evraklar nelerdir?"* başlığı altında. Yedi kalem + bir dışlama:
ürün ve **fiyat** bilgisi · Mesafeli Satış · SSL · iletişim bilgileri · teslimat şartları ·
gizlilik-güvenlik politikası; kapalı/yapım/test aşamasındaki siteler değerlendirmeye
alınmıyor. ⚠ **Tek açık kalem fiyat bilgisidir** — Yolculuk fiyat bandı kararlaşmadı
(`00-durum.md` YAYINI KİLİTLEYENLER md.4). ⚠ Ve dışlama maddesi **KARAR 575 ile gerilimlidir**:
denetim mock ekranla koşarsa denetçinin gördüğü şey tam olarak *"test aşamasında"*dır.
Karar değişmedi, gerilim kayda geçti.

**Bu turun kararları:**

- **KARAR 591 — FRAMEWORK MUHAFIZININ VARLIĞI KAPSAMINI GÖSTERMEZ:** Astro `checkOrigin`
  yalnız form-benzeri ya da content-type'sız isteği ölçüyor; dört iç endpoint'in dördü de
  JSON gönderiyor, yani muhafız onları **hiç ölçmüyordu.** Bu tur bir korumayı taşımadı —
  **olmayan bir korumayı kurdu.** `checkOrigin: false`'ın kaybettirdiği bir şey yok.
  Kapsam varsayılmaz, ölçülür.
- **KARAR 592 — ORIGIN KONTROLÜ KENDİ KODUMUZDA YAŞAR:** `security.checkOrigin` kapalı;
  `origin-muhafiz` üç iç route'a elle takılır, kural `origin-kural.ts`'de tek nüshadır.
  `/api/odeme-callback` **bilerek dışarıda** — dış origin'den POST alması gerekiyor.
  `/api/davet` de dışarıda: oradaki sessiz 200 bilinçli bir karardır.
- **KARAR 593 — KAYIT KİMLİĞİ YALNIZ HASH KAPSAMINDAKİ ALANDAN ÇÖZÜLÜR:** dönüş POST'u
  server-to-server değil, kullanıcının tarayıcısından gelir; imza kapsamı dışındaki her alan
  **tamamen saldırgan kontrolündedir.** Kaydı hash dışı bir alandan çözmek, imzanın koruduğu
  şeyi imzasız bir alana devretmektir — o zaman imza doğrulamasının anlamı kalmaz. Biçim
  tutmazsa fail-closed reddedilir ve gövdenin **alan adları** log'lanır.
- **KARAR 594 — ÖDEME ÖNCESİ TUTAR OTORİTESİ NOTION `Beklenen Tutar`'DIR:** pending satır
  açılırken yazılır; `/odeme/nkolay` ve callback onu okur. Alan boşsa ödeme **koşulsuz**
  reddedilir — kod istisna taşımaz, eksik veri elle doldurulur.
- **KARAR 595 — FAZLA TUTAR KABUL, DÜŞÜK TUTAR RET; RET SESSİZ OLMAZ:** taksit/komisyon
  farkı tutarı yukarı çekebilir ve reddetmek kadını *"ödedim ama kaydım kapanmadı"*
  durumunda bırakır. Reddedilen callback gerekçesiyle ve referansıyla log'a düşer —
  sessiz ret, para alınmış + kayıt Beklemede + ortada iz yok demektir.
- **KARAR 596 — DENEME SONEKİ ZAMAN DAMGASINDAN TÜRETİLİR:** `OCAK-XXXX-<epoch>`.
  Notion'a deneme sayacı **açılmaz** — ödeme otoritesi ikiye bölünmez. *"Kaçıncı deneme"*
  gerekirse N-Kolay `PaymentList`'ten okunur; orada her deneme zaten ayrı satırdır.
  Sonek yalnız giden alanda yaşar; Notion başlığına ve `/odeme/tamam?ref=` değerine sızmaz.

**Açık kalan körlükler:** B200 (mutabakat + iptal/iade yok) · B201 (`REFERENCE_CODE`
varsayımı) · B202 (gönderilen kod kaydedilmiyor) · B203 (`CURRENCY_CODE`).
Dördü de **bilerek** açık — kapatmanın ön koşulu pazartesi gelen dört anahtar.

---

## 2026-09-11 — MailerLite kayıt onayı turu · KARAR 597–598 · B kapanan: 194 · HEAD 67c7fd5

**Ölçümler.** Otomasyon `OCAK — kayıt onayı (tüm formatlar)`, id `196143397426169519`,
`enabled: false`. Üç mail: `196154492332278851` (ödeme bekleyen) · `196155049763669541`
(yüz yüze) · `196155489314146132` (online). Alan envanteri 20/12. Teslimat 12 gönderim /
6 teslim. Hesabın diğer otomasyonları: Ateş Mektupları welcome **açık**, Çember ve
Açık Kapı welcome **kapalı**.

**Yapılan.** Üç mailin gövdesi baştan yazıldı: logo (48px → 110px, ortalı, caption `OCAK`) →
H3 başlık → selamlama → köprü cümlesi → divider → "bilet" bloğu → divider → gövde → imza →
buton. Bilet satırları `hardBreak` ile bağlandı (Enter yeni paragraf açıyor, aralıklar eşitleniyordu).
Marka renkleri `10-marka.md` paletine getirildi: zemin `#1A1210` · metin `#F2EAE2` ·
kenar `#3D3532` · link `#D4A855` · buton `#C44B2F`. **`#ffffff` üç yerden kaldırıldı** —
buton yazı rengi, em dash karakterine yapışmış biçim, bir H3 başlığı.

**Eklenen içerik, ölçülmüş eksikler üzerine.** `etkinlik_tarihi` üç mailin **hiçbirinde
yoktu** — alan doluydu, kullanılmıyordu; kadın "yerin hazır" mailini alıp ne zaman
olduğunu bilmiyordu. Eklendi. Ödeme maili yalnız havale anlatıyordu; kart 10 Eyl'den beri
Production'da (KARAR 575) olmasına rağmen IBAN tek yoldu — kart satırı eklendi.
`{$etkinlik_basligi}` ve `{$etkinlik_adi}` etiketsiz iki çıplak satır olarak alt alta
duruyordu; ikincisi kaldırıldı.

⚠ **Düzeltilmiş yargı 1.** Tur içinde *"koşul `odeme_durumu = bekliyor` yerine `≠ alindi`
olsun"* önerildi ve **geri alındı.** `alindi`'yi hiçbir kod yazmadığı için `≠ alindi`
herkes için doğru olurdu ve `muaf` kadın da IBAN'lı ödeme mailini alırdı. Mevcut koşul
doğru çalışıyor. Null sorunu operatörle değil, test abonesi temizliğiyle kapanır.

⚠ **Düzeltilmiş yargı 2.** *"Boş `title` bloğu mailde ölü boşluk bırakıyor"* denmişti —
**yanlış.** JSON'dan okunmuştu, render'dan değil; editör placeholder'ı, boşken basılmıyor.
Aynı sınıf ikinci hata: *"footer Köz zeminli, dikkati aşağı çekiyor"* — panel
önizlemesine bakılmıştı, gerçek mailde footer koyu. KARAR 355/408'in mail yüzeyindeki
karşılığı: **durum builder'dan değil gönderilen mailden okunur.**

⚠ **Boşluk, kusur değil.** KARAR 547/549/558 sonrası Seremoni hazırlık akışı sayfadan
kalkıp kayıt sonrası kanala taşındı; karşılığı bir mail **yok**. Üç mailde doktrine aykırı
vaat de yok (kayıt alınır / geç kalırsan gel geçmiyor) — B165'in mail ayağı temiz, Resend
şablonları taranmadı.

**Bilinen sınırlar (ücretli plana bağlı):** footer `unsubscribe_text` İngilizce ·
"SENT BY MailerLite" rozeti. **Kabul edilen sapma:** font Georgia (MailerLite tek font
destekliyor, marka çifti uygulanamıyor).

⚠ **Ölçümde çıktı, uzlaştırılmadı (KARAR 102):** `00-durum.md` satır 3 dönem HEAD
`5ace816`, satır 72 tablosu `b5ff542`. Başlık tazeleniyor, tablo satırı tazelenmiyor —
bir önceki turun kalıntısı. Sahibi Kaan; ayrı turda kapanır.

- **KARAR 597 — MAILERLITE OTOMASYONUNUN TETİĞİ `etkinlik_adi`'DIR, `odeme_durumu` DEĞİL:**
  ödeme onayı akışı yalnız `odeme_durumu` yazarsa otomasyon **hiç tetiklenmez.** Ayrıca
  ödeme kapısı (KARAR 486) `zoom_link`'i kayıt anında boşalttığı için ikinci koşul
  `zoom_link is_not_provided` ödemeli **online** kaydı yüz yüze dalına atar — akış üç alanı
  da yazmak zorundadır.
- **KARAR 598 — MAİL YÜZEYİNDE FONT GEORGIA; SAPMA KABUL EDİLDİ:** MailerLite tek font
  destekliyor, markanın Cormorant Garamond + Outfit çifti uygulanamıyor. `10-marka.md`
  tipografi maddesinden **bilinçli ayrışma**, arıza değil.
