# build
dist
.astro
.vercel

# dependencies
node_modules

# logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# env — KARAR 97: token asla commit edilmez
.env
.env.local
.env.production
.env.*.local

# editor / OS
.DS_Store
.vscode/*
!.vscode/extensions.json
.idea
*.swp
*.swo

# vercel local cache
.vercel

# claude code — lokal operasyonel not (repo'ya gitmez)
.claude/

# notion envanter dump (geçici diff aracı, #29 Brief F.5 Adım 0)
scripts/notion-section-envanter.json

# scripts/*-dump.txt — dump çıktıları (Notion canlı, script yeniden üretir)
scripts/*-dump.txt

# ocak-site-icerik.md — site içerik snapshot (site-icerik-dump.mjs üretir, eskir)
/ocak-site-icerik.md
/docs/_uretilen/site-icerik.md

# Fable (copywriter) dump — tek seferlik, prod'dan üretilir; commit edilmez
ocak-site-dump-fable-*.md
.env*
!.env.example

# skill zip çıktısı (skill-sync.sh üretir, claude.ai'ye elle yüklenir)
/docs/_uretilen/skill-zip/
