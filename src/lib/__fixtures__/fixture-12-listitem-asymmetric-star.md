## section: test-asymmetric

Notion'da Cmd+B + literal `**`/`*` yazımı sonucu asimetrik 4/3 yıldız listItem:

- ****Bir geçişin içindesin*** — boşanma, iş değişikliği, kayıp.
- ****Eski araçların artık yetmiyor*** — terapi, kitap, podcast bir yere kadar.
- ****"Bir şey değişmeli"*** diyorsun.

Kontrol — sağlam markdown (sadece 3+3 yıldız) korunur:

- ***Doğru italik+bold*** — hiç dokunma.

Kontrol — düz paragraph başında `*` (listItem değil) — strip etme:

*Düz metin başında yıldız var ama bullet değil — bu farklı bir durum.
